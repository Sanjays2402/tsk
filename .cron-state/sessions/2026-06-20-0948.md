# 2026-06-20 09:48 PT — tick #7

5/5 shipped. Branch: feature/autoship. Pushed 031154a..5880aee.

| feature | sha | tests |
|---|---|---|
| `tsk pri --up/--down/--cycle` | 031154a | 4 new (cycle ladder, urgent wrap, conflict matrix, mode-required) |
| `tsk swap <id1> <id2>` | 4d6bcaa | 6 new (positions, round-trip identity, self-swap reject, unknown id, bad id, field preservation) |
| `tsk lint [--fix --json]` | 1daa4f3 | 10 new (clean file, non-canonical bullets, unknown meta, alias keys ok, missing created, stray notes, --fix canonicalizes, --fix drops unknown meta, JSON shape, empty findings, sorting) |
| `tsk rebuild-ids [--apply --yes --since-id N --json]` | af9ea6d | 9 new (dry-run no-op, apply densifies, --apply requires --yes, --since-id preserves lower, no-op clean, JSON shape, neg --since-id, planRebuildIDs reservation algorithm, summaryString helper) |
| `tsk bench [--iter N --json]` | 5880aee | 8 new (smoke run, JSON shape, --iter bounds, formatMicros boundary, humanBytes units, summarizePhase sort/empty, countLines edges) |

Gate: gofmt + vet + build + go test ./... all green before push.
Verified via `git log origin/feature/autoship | head -7` after push.

No deferred / no skipped. Roadmap status: tick #6 "Polish & DX"
subsection 9/10 shipped (only `tsk recent` deferred because the
name already aliases `log` — needs a new verb first). Added 10
fresh items in a "Polish & DX (added tick #7)" subsection.

Standout: `lint --fix` is the cleanest of the five — a safe
round-trip through store.Save canonicalizes everything the parser
tolerated, drops unknown meta keys, and the standard .bak chain
means `undo-last` reverts a surprise. The known-meta-keys set is
deliberately duplicated locally rather than exported from
internal/store, keeping the lint surface fully revertible.

Sibling structure now: doctor (in-memory) + lint (file bytes) +
bench (perf snapshot) + env + where = the full "what's going on
with my tsk" diagnostic surface.
