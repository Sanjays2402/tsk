# Web tick T31 — 2026-06-28 18:39 PT

Shipped 5/5 web slices on `main`, gated once. Canonical workdir mounted.
Theme: every Views-row + panel cohort readout turned interactive — recall,
sweep, and densest-jump are now one click from the at-a-glance text.

## Features
- F162 5d200d6 — panel cohort densest-jump ▲ mirrors F155's trail button.
  renderCohortPanelLine 4th optional arg; data-cohort-densest reused. +3.
- F159 ba78c25 — busiestHeadlineHTML wraps F154's "busiest: <name>" in a
  data-view-recall button (jump to the pile-up). innerHTML on viewsSummary.
- F164 ba78c25 — staleSweepSegmentHTML makes F163's "N stale" a data-views-
  sweep button (forget stale); same handler the standalone button fires.
- F165 ba78c25 — peekOpenOnlyLabel + peek-open:<id> command preview the open
  slice ("9 open · tags: #work") before the F158 recall-open jump.
- F166 ba78c25 — recallOpenOnlyTitle gains optional open "·N" suffix (F157 sib).
- F159/164/165/166 share one commit (interleave in views.ts+main.ts+css).
- bundle d? — rebuild (41 modules, 44.73KB gz JS, 11.29KB gz CSS).

## Gates
- web check 943->956 (+13), 0 fail; tsc app+test clean; build green.
- gofmt/vet/build clean; go test ./... green (commands 67s, rest cached).
- live serve GET / 200; JS carried views-summary-recall/-stale, stat-cohort-
  densest, "Peek open-only", "(open only)", peek-open:. Fixture cleaned.

## Deferred
Standing long-carries F48,F49,F50,F54. T32 backlog F167-F176 appended.

## Process note
Commit split: F162 isolated (cohort.ts), F159/164/165/166 grouped (shared
files). Toggle API smoke used wrong method (404/405) but irrelevant — all 5
slices are pure render/command, none touch /api; round-trip verified prior ticks.
