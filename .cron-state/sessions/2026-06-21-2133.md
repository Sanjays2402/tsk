# 2026-06-21 21:33 PT — tick #18

Shipped 5 features (4 commits — depend dry-run + CSV folded
together because the function-signature change carried both).

- 482c65b feat(depend): --remove-all accepts CSV ids and supports --dry-run
- 066f23d feat(start): tsk start --all with required --tag/--priority scope
- 5b5815f feat(graph): --highlight-tag spotlights every task carrying a tag
- 71ace7d feat(archive): --since-id <N> archives every Done task with id < N

Tests: 39 new (depend multi 7, depend dry-run 5, start --all 9,
graph highlight-tag 8, archive --since-id 10). Full gate green
(gofmt + vet + build + go test ./...) before push.

Deferred: none — the 5 shipped picks are all from the tick #17
backlog (plus archive --since-id from the freshly-added tick #18
ideas). 27 fresh items appended to keep future ticks fed.

Notes:
- Combined depend feature: CSV-id refactor (id int → ids []int)
  and dryRun bool both flow through the same runDependRemoveAll
  signature change. Splitting via index/reset would have been
  artificial. Both user-visible features fully tested
  independently.
- Caught + recovered from a `/System/Volumes/Data` 100%-full
  mid-tick via `go clean -cache -testcache` (freed 1.3Gi).
  Watch for recurrence; the Go build cache lives on the system
  volume and accumulates between ticks.
