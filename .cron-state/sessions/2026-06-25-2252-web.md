# T16 web tick — 2026-06-25 ~22:52 PT

Shipped 5/5 web slices (F86-F90) on main. Gated once at end, pushed clean
fc5f47d..910857b. Workdir /Volumes/Projects/tsk WAS mounted (no SSD fallback).

## Slices (each its own commit, cron identity)
- F86 ef00686 feat(web): priority-facet "in <lens>" scope note
- F87 9833f8c feat(web): "N waiting" dependent chokepoint badge on rows
- F88 4870b33 feat(web): persist export menu All/shown scope per session
- F89 b42d034 feat(web): palette disabled-reason hints for every gated command
- F90 f129918 feat(web): live lens digit-map mini-legend in the help overlay
- 910857b chore(web): rebuild embedded SPA bundle for T16 (JS 34.84KB gz)

## Gates (once, end of batch) — all green
- gofmt -l clean, go vet clean, go build clean
- go test ./... ok all packages (internal/commands 57.5s; internal/serve
  re-run uncached against the fresh T16 bundle, 2.3s)
- web npm run check: 607 pass (was 586; +21 net new), build ok (40 modules)

## Live proof
Built /tmp/tsk-t16, #4->#3->#2->#1 dep chain (so #1 is a chokepoint),
tsk serve :7896. /api/export?ids=1,4 -> exactly #1+#4 in store order;
whole-store -> all 4. Served bundle carries every hook (fprio-scope,
waiting-badge + data-waiting-walk x3, tsk.export.scopeShown,
nothing-to-undo / no-tasks-to-filter / already-on-all-tasks, lens-legend +
data-lens-legend + data-help-legend). Toggling #1 done round-tripped to
.tsk.md preserving the depends: chain + adding completed:; `tsk depend 4
--tree` read the full chain back. Storage contract intact.

## Deferred
Nothing forced. Standing long-carries: F48 (context-menu submenus), F49
(autocomplete in edit/filter), F50 (dep mini-graph), F54 (touch context menu).
T17 backlog F91-F95 appended to STATE-web.md.
