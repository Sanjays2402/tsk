# Tick #22 — 2026-06-22 11:54 PT

## Shipped 5/5

1. **`tsk graph --format svg`** — 58c69c7
   Self-contained SVG renderer (no GraphViz dep). Embedded
   layered-layout (Sugiyama-ish): leaf prereqs in layer 0, nodes
   sort by id ascending within layer, cycle-safe via bounded
   iteration. Honors --highlight/--dim/--highlight-tag/--dim-tag
   with the same styling intent as the DOT renderer for visual
   consistency. XML-escapes the 5 reserved chars in titles.
   7 table tests including XML-escape regression.

2. **`tsk archive --bucket-by id-range:N`** — 8956a97
   Id-axis fixed-width bucketing. Window labels "1-N", "N+1-2N"
   sort naturally via window-start sort key. id=0 → "id:0".
   Validates N > 0, non-integer, empty cases at exit 2.
   5 table tests.

3. **`tsk archive --bucket-by tag:X`** — 03f86fc
   Single-tag boolean partition. "tag:X" (sort=1, renders first)
   vs "other" (sort=2). Case-insensitive match, label preserves
   user case. Empty tag rejected at exit 2. No empty "## other"
   when all tasks match.
   5 table tests including case-insensitive matching regression.

4. **TUI 'r' reload from disk** — 61203c1
   Re-loads .tsk.md from disk without restarting the TUI.
   Preserves selection BY ID (not row index) across reload;
   falls back to position 0 if the previously-selected task
   was deleted externally. Resets transient editing state.
   Errors surface to status footer, never crash.
   4 table tests including selection-preservation regression.

5. **`tsk doctor --fix-orphans`** — 11f9f3f
   Scrubs dangling DependsOn refs from the archive (mirror of
   lint --autofix-all for the dep graph). Requires
   --check-orphan-archive (enforced at exit 2). Per-individual-ref
   counting matches warning granularity. Strips stale post-fix
   warnings from the in-memory report. Goes through archive.Save()
   so .bak snapshot is atomic (undo-last works).
   7 table tests including .bak-snapshot preservation regression.

## Quality gate

```
gofmt -l .         # clean
go vet ./...       # clean
go build ./...     # clean
go test ./...      # ok, ~58s total
```

All 5 commits landed on origin/main:
```
11f9f3f feat(doctor): --fix-orphans scrubs dangling DependsOn refs from the archive
61203c1 feat(tui): 'r' key reloads the .tsk.md from disk without restart
03f86fc feat(archive): --bucket-by tag:X boolean partition by single tag
8956a97 feat(archive): --bucket-by id-range:N groups archived tasks into fixed-width id windows
58c69c7 feat(graph): --format svg emits self-contained SVG (no GraphViz dep)
```

## Notes

- /Volumes/Projects was NOT auto-mounted at tick start (sparseimage
  not attached). Recovered with `hdiutil attach "/Volumes/Sanjay
  SSD/SanjayProjects.sparseimage"`. The first ls showed a stale
  cached subset (missing tsk dir); detach + re-attach fixed it.
  STATE.md already documents this footgun, but worth confirming
  the recovery path keeps working.
- gofmt re-aligned the const block in graph_svg.go after the
  initial commit. Included that whitespace cleanup in the doctor
  commit since it was incidental and a single block.
- All 5 features were genuinely independent (no shared types or
  cross-cutting code beyond resolveBucketByKey which #2 and #3
  both extend additively). Each commit is independently revertible.

## Deferred / future thought

- SVG `--output <path>` is a sensible follow-on (writes the file
  with extension validation instead of needing shell redirection);
  added to tick #22 roadmap.
- TUI 'R' (capital) for "reload AND clear filter" — sister of the
  lowercase r which preserves filter; added to roadmap.
- doctor --fix-orphans --dry-run would mirror every other fix
  verb's dry-run pattern; added to roadmap.
