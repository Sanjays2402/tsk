# Web tick T27 — 2026-06-28 07:40 PT

Shipped 5/5 web slices on `main`, gated once, pushed clean.

## Shipped (one commit each + bundle)
- F142 + F145 `599a7f3` — Views-row badge depth: busiestViewId (is-busiest
  amber marker) + countViewMatchesBreakdown/describeViewMatchBreakdown
  (open/done badge tooltip via the matchTitle resolver). main.ts hoists
  viewMatchPool() + viewMatchCounters() so F141 count / F145 tooltip / F142
  marker share one live pool. +14 views tests.
- F143 `dc25dbc` — formatCohortTrailMarkdown: Alt-click the trail copy button
  copies "#1 -> #4 -> #9" (arrow, distinct from F140's chevron). +6 cohort tests.
- F147 `486d719` — densestCohortAncestorIndex + Alt+D: leap to the heaviest
  cohort-drill ancestor (most live waiters). HELP_ROWS row. +4 cohort tests.
- F144 `ca400b8` — forget-all-stale: staleCohortViewIds + forgetStaleCohortsCommand
  (Cmd-K, new staleCohortCount ctx + "no stale cohort views" reason) + a
  "forget stale (N)" Views-row button; batch removal through the F134 leave-fade.
  +7 tests (3 views, 4 palette).
- `26affe9` — web_dist bundle rebuild (41 modules, JS 42.49KB gz, CSS 11.06KB gz).

## Gates
- web check 857 -> 882 (+25), 0 fail; tsc app + test clean; web build green.
- gofmt/vet/build clean; go test ./... green (commands 82.6s, serve cached-green).
- live serve smoke: GET / 200, all new hooks in the served JS, toggle
  round-tripped to .tsk.md preserving depends:1 + adding completed:. Storage
  contract intact.

## Deferred
F146 (peek-view-without-recall) -> T28. Standing long-carries: F48, F49, F50, F54.

## Workdir
The external SSD `/Volumes/Projects/tsk` WAS mounted this tick — worked
directly on the canonical path, clean fast-forward, no internal-worktree fallback.
