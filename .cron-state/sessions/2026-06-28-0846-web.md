# Web tick T28 — 2026-06-28 08:46 PT

Shipped 5/5 web slices on `main`, gated once, pushed clean `64d4f4d..db2843f`.
Canonical workdir `/Volumes/Projects/tsk` mounted (no internal-worktree fallback).

## Features (one commit each + bundle rebuild)
- F150 f8b5184 — cohort-trail segment waiter-counts (pure cohortTrailCounts +
  renderCohortTrail optional waiterCounts -> per-ancestor <sup>; main wires
  buildCohort counts; .cohort-trail-count CSS). +6 cohort tests.
- F151 60ce3b4 — stale-sweep undo (pure snapshotViews + restoreSweptViews;
  forgetStaleCohorts snapshots then shows a 6s Undo toast ->
  undoForgetStaleCohorts). +6 views tests.
- F148 47a4874 — Views-row coverage summary (pure viewsRowSummary +
  describeViewsRowSummary "N views · M tasks"; [data-views-summary] span;
  .views-summary CSS). +5 views tests.
- F149 0868ae2 — recall busiest view from the palette (pure
  recallBusiestViewCommand + hasBusiestView ctx/reason; currentBusiestViewId
  shares the chip-marker resolver; runCommand view-busiest -> recallView).
  +5 tests (4 palette + FULL_CTX field).
- F146 c13d455 — peek a view without recalling (pure peekViewLabel; peek:<id>
  commands; paintPaletteDuePreview renders into the preview slot; runCommand
  echoes to status). +4 views tests.
- db2843f — web_dist bundle rebuild (41 modules, JS 43.25KB gz, CSS 11.11KB gz).

## Gates
- web check: tests 882 -> 907 (+25), 0 fail; tsc app + test clean; build green.
- gofmt/vet/build clean; go test ./... green (commands 68.6s, serve fresh 2.7s).
- live tsk serve 127.0.0.1:7961: GET / 200; served JS carried every new hook
  (cohort-trail-count, "forgot " undo toast, views-summary, "busiest view",
  "Peek view"); POST /api/tasks/1/toggle round-tripped to .tsk.md preserving
  depends: links + adding completed:; tsk show 3 read depends:#1 back. Storage
  contract intact. Fixtures + binary cleaned up.

## Deferred
F152 (open/done badge-click filter action) — distinct row-interaction slice,
sized on its own. Standing long-carries: F48, F49, F50, F54.
T29 backlog (F152-F157) appended to STATE-web.md so the loop never starves.
