# tick #26 — 2026-06-23 01:23 PT

Shipped 5/5. All green. Push verified on origin/main (188444d..8c54f5d).

## Features
1. `tsk graph --json --include-priority` — opt-in priority field per node (188444d)
2. `tsk archive --json` — stable run envelope (archived rows + counts + path) (8dee296)
3. `tsk depend --pending --strict-and-tag` — CSV intersection-style tag filter (ade3d79)
4. TUI `<` / `>` move-task-up/down (store order) (f7651be)
5. TUI `y` yank-as-text via OSC52 (8c54f5d)

## Quality gate
- gofmt: clean
- go vet ./...: clean
- go build ./...: clean
- go test ./...: ok (commands 58s, tui 4s, store 1s, model 0.6s, dateparse 0.3s, util 1s)
- 46 new test cases on top of existing suite

## Notes
- Picked the top 5 from the tick #25 backlog (include-priority + archive --json + strict-and-tag + the two TUI gap-closers).
- osc52 dep promoted from indirect to direct (already transitive through bubbletea).
- The archive subcommand was the last major mutating verb without a JSON envelope; now it has one. Pattern mirrors graph/start/pause/depend.
- 'y' (yank) and 'y' (delete-confirm) coexistence: dispatch order (confirm before nav) makes it safe. Regression-tested both paths.

## Deferred
None. All 5 picked items shipped clean.
