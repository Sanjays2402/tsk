# tsk autoship — tick #19 (2026-06-22 00:45 PT)

Shipped 5/5 features as 5 clean single-commit slices, all single-commit
per feature, full quality gate (gofmt + vet + build + go test ./...) green
before push. Pushed 7e4ae2e..f9e8674 to origin/main; verified landed.

## Shipped

| Feature | SHA | Commit subject |
|---------|-----|----------------|
| graph --dim ids | 7e4ae2e | feat(graph): --dim <ids> inverse of --highlight to push nodes to the background |
| graph --dim-tag | 616ff42 | feat(graph): --dim-tag <name> tag selector for the dim verb |
| graph --highlight-tag CSV | 62e6da7 | feat(graph): --highlight-tag accepts CSV (union of multiple tags) |
| start --all --dry-run | 6c9efc4 | feat(start): tsk start --all --dry-run previews bulk-start without writing |
| pause --all --tag/--priority | f9e8674 | feat(pause): tsk pause --all --tag/--priority narrows the bulk-pause |

## Test coverage

- graph --dim: 11 new test cases
- graph --dim-tag: 7 new
- graph --highlight-tag CSV: 7 new + 1 unit test on splitTagCSV
- start --all --dry-run: 9 new
- pause --all --tag/--priority: 9 new
- **TOTAL: 44 new test cases** on top of the existing suite (~860 cases), all green.

## Refactors landed (silent — no separate commits)

- `parseHighlightCSV` body → shared `parseCSVIDSet(s, raw, flagName)` so
  `parseDimCSV` reuses the parser with flag-name-aware error messages.
- `mergeHighlightTag` body → shared `mergeTagIntoSet` (feature 2), then
  generalized to `mergeTagsIntoSet` (feature 3) so the single-tag and
  CSV forms collapse into one implementation. Public wrappers
  `mergeHighlightTag`/`mergeDimTag` are now one-line delegators.
- `emitGraph` / `printGraphDOT` / `exportGraphDOT` signatures all
  grew the dimSet/dimTag parameters in lockstep. The reachable.go
  caller updated to pass `nil` for both highlight and dim (its scope
  doesn't accept either flag).
- `runPauseAll` grew tag/priority params; backward-compat regression
  test asserts no-filter behavior unchanged.
- `runStartAll` grew dryRun param; idempotent-aware "would-start"
  computation mirrors what the non-dry path actually does (already-
  started tasks excluded unless --reset).

## Process notes

- No fixups or amends needed; each feature landed as a single clean
  commit on the first pass.
- Backtick gotcha in feature 3's Long help: tried to render literal
  shell-style 'release,p0' in backticks inside a Go raw-string. Raw
  strings can't contain backticks. Fixed by switching the inline
  literal to single quotes. Worth remembering: in Long: help strings
  delimited with backquotes, no internal backticks anywhere.
- Order of features matters: shipping --dim first set up the shared
  parseCSVIDSet helper which fed into the next two graph features
  cleanly. The mergeTagIntoSet → mergeTagsIntoSet evolution likewise
  happened naturally because feature 3 needed CSV semantics.

## Roadmap delta

- "Polish & DX (added tick #18)" subsection: 4/27 → 9/27 (the 5
  shipped this tick).
- Added "Polish & DX (added 2026-06-22 tick #19)" subsection with 25
  fresh items — TUI work, recurring tasks, config + multi-file, plus
  follow-ons from this tick's features.

## Deferred (none — shipped the full 5)

Quality bar maintained: each feature is a real user-facing capability
with tests, help text, error messages, and integration through both the
primary command and the export verb where applicable. No padding, no
splitting one feature into fake-multiple.
