# T22 web tick — 2026-06-27 ~06:35 PT

Shipped 5/5 web slices on `main` (F117-F121), gated once, pushed clean
`b9926ae..15152de`.

## Shipped

- **F117** `d8fb270` Cohort breadcrumb in the help overlay (renderCohortHelp in
  cohort.ts; [data-help-cohort] slot in toggleHelp; app.css). +6 tests.
- **F118** `39361ee` Depth-aware Clear cohort label (clearCohortCommand gains
  historyDepth=0 -> " + N-step history"; main.ts passes cohortHistory.length). +5.
- **F119** `190e3f0` Flash the just-pinned chip (ViewChipOpts.flashId + is-flash;
  pendingPinFlashViewId set in pinCurrentLens, cleared in renderViewsRow;
  @keyframes view-chip-flash + reduced-motion fallback). +3.
- **F120** `fed4326` Pinned-lens state synced across help line + digit map
  (renderActiveLensHelp pinned arg + renderLensDigitMap pinnedKind; one
  findPureLensView in toggleHelp drives both; app.css). +6.
- **F121** `88e217f` Chokepoint-shift toast on live reload (chokepointShiftMessage
  in stats.ts; liveRefresh(deferred) wrapper both live paths route through,
  throttled 8s; manual refresh never toasts). +3.
- `15152de` bundle rebuild (41 modules, JS 39.08KB gz, CSS 10.46KB gz).

## Gates

- web `npm run check`: 768/768 tests pass (747 -> 768, +21), both tsc clean.
- Go: gofmt clean, vet clean, build clean, `go test ./...` all ok
  (commands pkg 66.3s; serve re-run -count=1 clean against the fresh bundle
  2.6s; store/model/dateparse/tui/util cached-green).
- Live `tsk serve --addr 127.0.0.1:7922 --file /tmp/t22.tsk.md`: GET / 200; all
  6 JS hooks + 4 CSS hooks present in the served bundle; GET /api/tasks 200;
  POST /api/tasks/1/toggle (completes #1 -> biggest chokepoint shifts #1->#2,
  the exact F121 scenario) round-tripped to .tsk.md preserving every depends:
  link + adding completed:; `tsk show 3` read depends:#1 back; raw .tsk.md
  stayed plain hand-editable markdown. Cleaned up the /tmp fixture + binary.

## Deferred

Nothing forced. Standing long-carries: F48 (context-menu submenus), F49
(autocomplete in edit/filter), F50 (dep mini-graph), F54 (touch context menu).
T23 backlog (F122-F126) appended to STATE-web.md so the loop never starves.
