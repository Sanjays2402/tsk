# tick #17 — 2026-06-21 18:31 PT

Shipped 5/5 features. All single-commit, all tests green, full
quality gate (gofmt + vet + build + go test ./...) clean before
push. Pushed a22f9be..7e86892 to origin/main; verified landed.

## Shipped

- `pause --all`                  — a22f9be feat(pause): clear every in-progress task at once
- `archive --strategy quarterly` — 6c4f294 feat(archive): Q1/Q2/Q3/Q4 sections
- `archive --strategy yearly`    — 29f5ead feat(archive): one section per calendar year
- `graph --highlight csv`        — 6d12a34 feat(graph): comma-separated id list for multi-spotlight
- `depend --remove-all`          — 7e86892 feat(depend): global scrub of id from every dep list

## Notes

- The bucketed archive family is now COMPLETE: flat / daily /
  weekly / monthly / quarterly / yearly. The
  writeBucketedArchive(path, arch, batch, bucketFn) scaffolding
  from tick #14 turned out to be the right shape — each new
  strategy is just a new bucketFn + a case in the switch.

- `graph --highlight` changed from int to string CSV. Backward-
  compat is intact for single-id usage. Multi-id sets render
  every member with the SAME gold/bold spotlight style so a
  related cluster reads as one highlighted group.

- `depend --remove-all` is a global SWEEP, distinct from
  `--remove`/`--clear` which curate one task's list. Pairs cleanly
  with `tsk rm`/`tsk merge` so callers don't have to spelunk
  for dependents.

- `pause --all` is the end-of-day shortcut. Mutex with positional
  ids — combining them would hide typos like
  `tsk pause --all 3` plausibly meaning "everything except 3".

## Process

- No fixups or amends needed this tick. Each feature landed as
  a single clean commit on the first pass.
- Two pre-existing tests used "quarterly" as their bogus-strategy
  probe (TestArchiveStrategyWeeklyRejectsBogusValue,
  TestArchiveStrategyBogusValueErrorMentionsMonthly). Swapped to
  "nonsense" — folded into the quarterly feature commit since
  the swap is meaningless without quarterly being a real option.

## Deferred

None. Five clean features shipped from the tick #16 backlog.
