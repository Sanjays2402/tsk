# tick #6 — 2026-06-20 06:40 PT

5/5 features shipped, all single-commit revertible, full quality
gate green (gofmt + vet + build + go test ./...) before push.
Pushed 865aefe..50e6aa6 to origin/feature/autoship; verified
landed via `git log origin/feature/autoship | head`.

| feature                | SHA      | notes                                                                              |
| ---------------------- | -------- | ---------------------------------------------------------------------------------- |
| pin/unpin              | 31eaa5a  | new pin:true meta key, additive format, next/top now respect pinned-first ordering |
| last                   | 6578af4  | most recently mutated task; synthetic-ts unit tests + light CLI smoke              |
| man                    | cf87073  | cobra/doc GenManTree; --install --yes for /usr/local; ./man default                |
| completion --install   | be6973d  | bash/zsh/fish file-drop; powershell sentinel-guarded block (idempotent)            |
| wait                   | 50e6aa6  | new WaitUntil field; default-hidden from ls/top/next; --list to see queue          |

No deferrals. No partial work. Roadmap status: per-task CLI shortcuts
(10/10) + new views/scriptable surfaces (9/9) + 1 storage/format item
all done; Polish & DX 5/10 shipped this tick; added a new 10-item
"Polish & DX tick #6" subsection so the next 2-3 ticks have sized
work in the queue.

Notable: had to absorb one tiny gofmt fix on completion_test.go from
my own struct-alignment edit. Used `git commit --fixup` +
`GIT_SEQUENCE_EDITOR=: git rebase -i --autosquash` to fold it into
the completion commit cleanly — kept history one-commit-per-feature.

Tests added this tick: ~30 across the 5 features. 0 regressions in
the existing 200+.
