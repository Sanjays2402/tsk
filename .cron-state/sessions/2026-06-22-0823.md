# tick #21 — 2026-06-22 08:23 PT

Branch: main. Shipped 5/5 features directly on main.

## Slate (all shipped)

1. `tsk graph --reachable/--upstream-of --json` (04cb7c8) — stable
   subgraph envelope for scripted impact-analysis. Shared
   emitSubgraphJSON between both directions; envelope includes
   root_id, direction, nodes[], edges[], optional filter.
   Tests: 10. Closes 2 tick #20 roadmap entries with 1 feature.

2. `tsk lint --autofix-all --json` (4184919) — folds findings +
   repair summary into ONE coherent JSON document. Fixes the
   legacy interleaved JSON+text shape that broke jq pipelines.
   Read-only --json keeps its old shape for backward compat.
   Tests: 9 new + 1 updated.

3. `tsk lint --autofix-all --backup --keep N` (34ba194) — prunes
   the backup chain to the N newest snapshots. Pattern-scoped
   (only "<base>.bak.YYYYMMDD-HHMMSS" eligible; user-renamed and
   unrelated files preserved). Tests: 10.

4. `tsk doctor --check-orphan-archive` (1537cb9) — doctor's
   first cross-store check. Scans .tsk.archive.md for
   DependsOn refs missing from BOTH live and archive. Warning
   level (dangling deps aren't parse failures). Tests: 9.

5. `tsk archive --bucket-by priority|tag` (797be8c) — non-time
   bucket axis. Priority (urgent → low, descending order) and
   tag (first-tag, untagged → "## untagged"). Mutex with
   --strategy. Composes with --merge-into and --dry-run. Tests: 10.

## Stats

Commits pushed: 5 (04cb7c8..797be8c)
New tests: 48 (across 5 features)
Full gate: green (gofmt + vet + build + go test ./... at 49.6s)
Roadmap completion: tick #20 list 5/31 → 10/31; 28 fresh items
added under tick #21 subsection.

## Notable

- One commit identity bug caught and amended: extra '+' in
  noreply email (Sanjays2402+@) reset via --reset-author with
  the canonical id (51058514+Sanjays2402@users.noreply.github.com).
- TestLintAutofixAllJSONReportShape (existing) had to be updated:
  it asserted the OLD interleaved JSON+text shape (a bug, not a
  feature — broken jq pipelines). The new envelope is the
  contract.
- Two test bugs caught + fixed before gate: (1) `tsk done` rejects
  a task with open prereqs (documented footgun); reordered to
  done-then-depend in the doctor orphan clean test. (2) Parser is
  permissive about content errors; replaced a "corrupted archive
  is Error" test with a more useful "live store references archive
  id resolves" regression.

## No deferred items.
