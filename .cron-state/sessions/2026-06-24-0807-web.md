# T4 web tick — 2026-06-24 08:07 PT — power user (5/5)

## Workdir
Canonical `/Volumes/Projects/tsk` (external SSD sparseimage) NOT mounted again
this tick — `/Volumes` had only Macintosh HD; `diskutil mount Projects` failed
("Failed to find disk"). Worked from the synced internal worktree
`/Users/sanjay/Projects/tsk-features/main` (origin/main 69b49f8, clean tree,
.cron-state tracked in git). Clean fast-forward push 69b49f8..2ee4265.

## Shipped (5/5 + bundle chore)
- F16 27d23eb feat(web): bulk select rows for multi-toggle / multi-delete
- F17 88308ff feat(web): drag-to-reorder tasks, persisted to .tsk.md
- F18 277498c feat(web): Cmd-K command palette, fuzzy find + run any action
- F19 560db29 feat(web): export tasks as JSON / CSV / Markdown
- F20 3e8d053 feat(serve): optional bearer-token auth for off-loopback serving
- chore 2ee4265: rebuilt embedded SPA bundle

## Backend
- store.Move(moved, before) + POST /api/tasks/:id/move (F17) — 7 store + 4 serve tests
- GET /api/export?format=json|csv|markdown w/ Content-Disposition (F19) — 8 tests
- requireAuth + tokenBootstrap middleware, constant-time token, HttpOnly cookie,
  ?token= rejected on /api/* (F20) — 11 tests

## Frontend (pure modules, node --test)
bulkselect.ts (16), reorder.ts (14), palette.ts (14), export.ts (6) = 50 new
(156 total). sections.ts comparePriority now stable on file order so drags stick.

## Gates (once, end of batch)
gofmt -l clean; go vet clean; go build ok; go test ./... ok ALL packages
(commands 56.9s, serve with fresh embed); web npm run check 156 pass;
npm run build ok — JS 14.0KB gz, CSS 5.5KB gz, 22 modules.

## Live E2E (tsk serve --addr 127.0.0.1:7891 --token testtok123)
- no token /api/tasks -> 401 + WWW-Authenticate
- Bearer -> 200 list
- ?token= on /api/* -> 401 (rejected by design)
- POST /api/tasks/3/move {before:1} -> charlie,alpha,bravo; .tsk.md rewrote to that order
- export csv/markdown -> correct shapes, priority glyphs, Content-Disposition
- GET /?token= -> 303 clean / + HttpOnly tsk_token cookie -> /api/tasks 200
- SPA shell public (200 no token)
- served bundle contains data-bulk-toggle, reorder, data-cmd-id, data-export-format, /move

## Deferred
None — 5/5 solid. F21-F22 (SSE, PWA) queued for T5; F23-F30 appended for T6+.
