# Web tick T37 — 2026-06-29 12:06 PT

Shipped 5/5 web slices on main. Gated once. Pushed clean fffb250..a0065eb
(F199 5819917, F201 e22b299, F202 2a3b7b9, F203 f16e8d7, F205 f911115,
bundle a0065eb).

Rounded out the Views-row portability + legibility story:
- F199 (5819917) — importViewsDocAfter: position-aware paste that lands the
  merged view(s) right AFTER the anchor chip (de-dup/fresh-id/garbage byte-
  identical to importViewsDoc; null anchor === append). Per-view "Paste view(s)
  after (<name>)" Cmd-K command, preview+confirm+undo, names the anchor.
- F201 (e22b299) — viewGroupSizeBefore + renderViewDivider(label,count?) folds
  "#work (3)" into the cluster heading (+ title/aria); renderViewChips
  dividerCount opt; main.ts dividerCountFor paired with dividerFor. CSS added.
- F202 (2a3b7b9) — staleSweepSegmentHTML gains oldestDays → "N stale · Md" on
  the sweep button face (0="today", neg/omitted=no suffix). main.ts feeds max
  over the SAME staleAged clock the F179 tooltip uses. CSS added.
- F203 (f16e8d7) — router "shared" route + "view=" prefix; base64url encode/
  decode (TextEncoder/btoa + Buffer fallback, mangled→all-tasks never throws);
  copyShareLink(id) + per-view "Copy share link" command; applySharedViewDoc
  inbound (preview+confirm+undo, clears hash) on onRouteChange + boot.
- F205 (f911115) — copyRowShareLink(): whole-row link via the SAME shareLinkURL
  + clipboard path; static "Copy share link (all views)" command. Decode reuses
  F203's importViewsDoc path (1 or N views identical).

Commit grouping: 5 SEPARATE source commits + 1 bundle commit. Verified LOSSLESS
by reconstruct-and-diff: after committing all slices + bundle, the working tree
was byte-IDENTICAL to the full validated working copy across all 6 source files.
Each commit independently revertible.

Gates: web 999→1014 tests (+15: 9 views, 6 router), 0 fail; tsc app+test clean;
build green (JS 47.90KB gz, CSS 11.57KB gz, 41 modules). gofmt/vet/build clean;
go test ./... green. live serve 8779 GET / 200; served JS+CSS carried every new
wiring string; POST /api/tasks/1/toggle round-tripped [ ]→[x]→[ ] to .tsk.md
(timestamps + hand-editable format preserved).

Dropped/deferred (3, honest):
- F200 (copy-row toast parity) DROPPED — already satisfied by F188; copyViews
  already toasts the count. A separate commit would be a no-op, not a feature.
- F192 (archive pane) DEFERRED — large structural surface (pane+route+render);
  its own design tick, carried to T38.
- F191 (recall-open kbd binding) DEFERRED — needs a real shortcut bound before
  the hint badge can be truthful; carried to T38.

Standing long-carries: F48, F49, F50, F54, F168, F169, F173, F174.
T38 backlog F206-F212 + carries appended. Disk: Projects volume OK.
