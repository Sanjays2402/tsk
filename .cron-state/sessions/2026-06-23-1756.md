# tick #30 — 2026-06-23 17:38 PT

5/5 shipped. Full quality gate green (gofmt + go vet + go build +
go test ./... — 65.6s, all packages clean).

Commits pushed to origin/main:
- b3e4cc8 feat(wip): --priority <p> filters in-progress tasks by exact priority
- 579ebd2 feat(wip): --tag <t> filters in-progress tasks by single tag
- a7fa477 feat(wip): --strict-and-tag CSV intersection tag filter
- 165c28c feat(graph): --json --filter-started-since trims to recent in-progress
- e2a07e4 feat(graph): --json --filter-touched-since ergonomic UNION shortcut

43 new test cases (8+7+8+11+9) on top of the existing suite, all
green. Three new test files in internal/commands/:
- wip_priority_test.go
- wip_tag_test.go
- wip_strict_and_tag_test.go
- graph_filter_started_since_test.go
- graph_filter_touched_since_test.go

Modified:
- internal/commands/start.go (newInProgressCmd grows three filter
  flags + parsing + filter loop + buildWipFilterSummary helper)
- internal/commands/graph.go (filterStartedSince +
  filterTouchedSince vars; UNION composition in emitSubgraphJSON;
  FilterStartedSince doc field; flag declarations + Examples)
- internal/commands/wip_stale_test.go (two pre-existing
  assertions updated for the generalized header wording)

Process notes:
- One amend fix: the third commit (strict-and-tag) initially used
  a typo'd noreply email (`Sanjays2402+@` instead of
  `Sanjays2402@`). Caught immediately, amended with --reset-author
  before push. Future ticks: cat $msg / verify email arg before
  invoking commit.

- The `tsk wip` filter cluster is now COMPLETE (stale + priority
  + tag + strict-and-tag), matching the surface area of
  `tsk depend --pending` and `tsk pause --all`. All four flags
  compose as AND. The four bulk-verb-adjacent verbs are
  finally symmetric on the strict-and-tag axis.

- The graph JSON envelope now exposes THREE recency-filter axes
  (completed-since, started-since, touched-since), plus the six
  opt-in node fields + --include-all shortcut. UNION composition
  for the two precise filters (only useful semantic since done
  and in-progress are mutually exclusive on the same task). The
  touched-since shortcut sets both markers on the envelope —
  no new field added, on-disk shape backward-compatible.

- Quality gate ran ONCE for the whole batch. No deferred items.
