# Web tick T25 — 2026-06-27 20:17 PT

Shipped 5/5 web slices on `main` (F132-F136), gated once, pushed clean
`288ce9c..3214dfd`.

## Features
- **F132** `38e6be7` — cohort drill breadcrumb trail in the stats panel. cohort.ts
  renderCohortTrail(focus, history) renders the whole back-stack as "#A › #B ›
  #current" (ancestor steps = data-cohort-jump buttons, current = inert
  aria-current span); jumpCohortHistory(tasks, stack, index) leaps to any
  ancestor by running popCohortHistory over stack.slice(0, index+1) (reuses
  skip-dead-ancestor). main.ts cohortJumpTo(index), trail rendered above the F126
  line, click routes data-cohort-jump FIRST. app.css .cohort-trail (wraps).
  +9 cohort tests.
- **F133** `a017373` — pin a cohort as a re-derivable saved view. A cohort id-set
  isn't serializable, so a cohort view stores ONLY the chokepoint sourceId and
  re-derives via buildCohort on recall. views.ts SavedView.cohort? + normalizeViews
  carry + isCohortView/findCohortView/addCohortView (overwrite-by-chokepoint-or-
  name); renderViewChips cohort chips active only when activeCohort===cohort (not
  filter equality), ↑ glyph + is-cohort-pin; describeView names the chokepoint.
  cohort.ts renderCohortPanelLine grows a trailing ★/☆ pin star (data-cohort-pin).
  main.ts togglePinCohort (pin/unpin), recallView routes cohort views through
  setCohort, refreshStats passes pinned flag, renderViewsRow passes activeCohort +
  ↑ glyph, panel click routes data-cohort-pin before clear. Design note recorded
  in STATE-web FIRST (per prompt). +18 tests (views + cohort).
- **F134** `f57a2fb` — every saved-view chip fades out on removal. Extracted
  animateChipExitThenRemove(viewId, done) — marks the chip `is-leaving`, defers
  removeView by UNPIN_EXIT_MS (synchronous in test env via canAnimateChipExit).
  deleteView (chip ×), unpinCurrentLens (lens), togglePinCohort unpin (cohort) all
  route through it. CSS .is-unpinning -> neutral .is-leaving (alias kept so nothing
  breaks). No new pure logic (seam already covered).
- **F135** `6acf1ca` — `f` key mirrors the shift-toast Focus action. stats.ts
  toastFocusAction(focusId, statsOpen) — single source of truth for "what Focus
  does" (null / {focusId, revealPanel}), used by BOTH the toast button and the key.
  main.ts liveToastFocusId slot (set in liveRefresh, cleared on dismiss/timeout/
  action), runLiveToastFocus (setCohort + guarded reveal + dismiss), dismissInfoToast,
  `f` keydown (no-op when no live focus), HELP_ROWS row. +4 stats tests.
- **F136** `e5d0d64` — keyboard pin/unpin from the lens star's own focus. lens.ts
  lensStarKeyAction(key, shift, kind, pinned): pin on plain Enter/Space, unpin on
  Shift+activation when pinned, none otherwise (distinct from the * toggle — star
  pins on activation, unpins only with Shift, mirroring click vs right-click).
  main.ts star keydown preventDefaults a handled activation key so the native
  button click can't double-fire pinCurrentLens. +4 lens tests.
- `3214dfd` — bundle rebuild (41 modules, JS 40.84KB gz, CSS 10.91KB gz).

## Gates
- web tests: 799 -> 829 (+30), 0 fail
- tsc app + test/tsconfig.json: clean
- gofmt clean / go vet clean / go build clean / go test ./... all ok
  (commands 66.5s, rest cached-green)
- live `tsk serve --addr 127.0.0.1:7926`: GET / 200; served bundle carried all
  new hooks (cohort-trail, data-cohort-jump, data-cohort-pin, is-cohort-pin,
  is-leaving, "waiting on #"); GET /api/tasks exposed depends_on:[1] for the
  waiters; POST /api/tasks/1/toggle round-tripped to .tsk.md preserving BOTH
  depends:1 links + adding completed:; `tsk show 2` read depends:#1 back.
  Storage contract intact. Fixtures + binary cleaned up.

## Deferred
Nothing. Standing long-carries: F48 (context-menu submenus), F49 (autocomplete in
edit/filter), F50 (dep mini-graph), F54 (touch context menu).

## Next
T26 backlog F137-F141 appended to STATE-web.md (cohort trail keyboard nav, stale
cohort-chip self-clean, Cmd-K pin-cohort command, trail copy-chain, saved-view
match-count badges).
