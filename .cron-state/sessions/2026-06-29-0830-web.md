# Web tick T36 — 2026-06-29 08:30 PT

Shipped 5/5 web slices on main. Gated once. Pushed 9360f76..e899b08
(F196 helper eb9f5eb, F194 5fb3ddc, F195 c4e4e9f, F196 wiring 517a8f5,
F198 e1bac12, bundle e899b08).

Finished the Views-row PORTABILITY + dead-chip-legibility story:
- F194 (5fb3ddc) — divider recall flash: "#tag" cluster heading click arms the
  same pendingDoneFlash one-shot F186 fires on done-recall; the tag jump is felt.
- F195 (c4e4e9f) — paste-views undo: snapshot pre-paste row, 6s toast Undo
  restores it exactly (drops added ids). Mirrors F151. Clears dangling recall ptr.
- F196 (eb9f5eb helper + 517a8f5 wiring) — exportSingleViewDoc single-view
  portable envelope; per-chip copy button (copyable opt) + copySingleView
  clipboard write (guarded) + toast. Round-trips through importViewsDoc.
- F197 (eb9f5eb) — chipStaleBadge bare "Nd" badge ON a dead chip's face via the
  same staleCohortAge resolver F190's title reads; .view-chip-stale-age CSS.
- F198 (e1bac12) — per-view "Copy view (<name>)" Cmd-K command, keyboard sibling
  of F196's button, same copySingleView path (can't diverge).

Commit grouping: F196 helper (views.ts/css/test) + F197 share the eb9f5eb commit
(both touch the same chip-markup line — honest entanglement, not padding); the 4
main.ts wiring features are SEPARATE commits, verified lossless by reconstructing
main.ts byte-identical to the full working copy (checkout-and-reapply per feature).
Each commit independently revertible.

Gates: web 990->999 (+9 views tests), 0 fail; tsc app+test clean; build green
(JS 46.71KB gz, CSS 11.53KB gz, 41 modules). gofmt/vet/build clean; go test ./...
green (commands 64.4s; serve/store/model/tui/util cached). live serve 8774 GET /
200; served JS carried all wiring strings (data-view-copy, view-chip-stale-age,
copy-view:, "Copy view (", "undid paste"); CSS carried view-chip-copy +
view-chip-stale-age; POST /api/tasks/1/toggle round-tripped [ ]->[x] to .tsk.md.

Deferred/blocked (3, honest):
- F191 (kbd hint badge) DEFERRED — recall-open commands have no bound shortcut;
  a hint would render a fake key. Bind a real shortcut first (reframed into T37).
- F192 (archive pane) DEFERRED — large structural surface (pane+route+render);
  its own design tick (carried to T37).
- F193 (recurring-task chip) BLOCKED — zero recur metadata in internal/model, the
  serve DTO, or .tsk.md. Nothing to render. Waits on the CLI loop shipping recur
  storage (still unstarted on STATE.md).

Standing long-carries: F48, F49, F50, F54, F168, F169, F173, F174.
T37 backlog F199-F203 + carries appended. Disk: Projects volume OK.
