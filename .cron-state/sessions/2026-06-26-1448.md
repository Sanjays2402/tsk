# Web tick T19 — 2026-06-26 ~14:48 PT

Shipped 5/5 web slices on `main`, gated once, pushed clean (3e524cd..6f00a44).

## Shipped
- **F101** `7da6470` Persist cohort focus across live-reload — cohort.ts
  reconcileCohort + CohortReconcile; refresh() reconciles focusCohort (live id
  set or drop-with-toast). +6 tests.
- **F102** `5214994` Cohort focus from any chokepoint you're walking —
  renderCohortFocusButton (shared data-cohort-focus hook); "focus these" in the
  dependent chain-drill head; popover click -> setCohort. app.css .chain-pop-focus.
  +2 tests.
- **F103** `d1c2f30` Cohort Cmd-K commands — cohortSummary; clearCohortCommand +
  focusChokepointCommand; CommandReasonContext +hasCohort/+hasChokepoint;
  currentChokepointId(). +9 tests.
- **F104** `4120a59` Lens+facet saved-view bridge — SavedView.lens (optional,
  back-compat); addView/updateView/normalizeViews lens-aware; viewMatches +
  activeViewWithLens; describeView + renderViewChips (is-lensed); saveCurrentView
  captures activeLens, recallView re-applies via parseLens; lens-aware
  savable/dup/updatable. app.css .view-chip.is-lensed. +15 tests.
- **F106** `40c49e8` "Other bottlenecks" ranked chokepoint list (IN PLACE OF
  F105) — deps.topChokepoints ([0]==biggestChokepoint); stats.renderOtherChokepoints
  (reuses walk + cohort-focus hooks); renderStatsPanel optional ranked list;
  refreshStats passes top 6. app.css .stat-choke-*. +8 tests.
- `6f00a44` bundle rebuild (41 modules, JS 37.36KB gz, CSS 10.10KB gz).

## Deferred
- **F105** (queued) — FILLER. The empty-selection bulk guard protects an
  unreachable state: openBulkEdit returns early at `bulk.ids.size === 0` and the
  bar is `hidden` at count 0 (spec only cited a "race"). Per the no-padding rule,
  deferred and shipped F106 (real triage feature) instead, keeping 5 great slices.

## Gates
- Go: gofmt clean, vet ok, build ok, `go test ./...` ok (commands pkg ~56s).
- Web: `tsc --noEmit` + `tsc -p test/tsconfig.json` both clean; 700/700 tests
  (666 -> 700, +34 net new).

## Live proof
Built /tmp/tsk-t19-test via CLI — two chokepoints (#1 Ship release w/ 4 waiters,
#2 Deploy infra w/ 2). `tsk serve --addr 127.0.0.1:7919`; served bundle carried
every F101-F106 hook (JS + CSS verified). POST /api/tasks/1/toggle round-tripped
to plain .tsk.md preserving all depends: links + adding completed:; `tsk show 3`
read depends:#1 back. Storage contract honoured.

## Next
T20 backlog F107-F111 appended (keyboard-focus ranked chokepoints, cohort
back-stack, recalled-view lens chip, one-click save-lens, chokepoint trend hint).
