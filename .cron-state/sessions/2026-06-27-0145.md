# T21 web tick — 2026-06-27 ~01:45 PT

Shipped 5/5 web slices on `main` (F112-F116), gated once, pushed clean
`419fd19..909741b`.

## Shipped

- **F112** `59356cb` Pure-lens bookmarks read as lens chips in the Views row
  (isPureLensView + lensGlyph resolver in views.ts; is-lens-pin + glyph span;
  main wires parseLens+lensMeta; app.css suppresses ◈, tints glyph). +6 tests.
- **F113** `fb38719` Cohort back-stack depth badge (renderCohortChipBody shows
  the depth numeral on the ‹ glyph when history >1; behaviour unchanged). +3.
- **F114** `d768c5b` Cmd-K leads with the just-shifted biggest chokepoint
  (focusShiftedChokepointCommand; main tracks the shift in refresh() via two
  new slots independent of the panel-gated F111 slot; maybeCommand spread;
  cohort-focus-new dispatch). +6.
- **F115** `370e97c` Pin the active lens from Cmd-K (pinLensCommand pin/recall
  title; lens-pin disabled-reason; runCommand -> pinCurrentLens). +6.
- **F116** `5a94266` Lens provenance in the help overlay's active-lens line
  (renderActiveLensHelp optional provenance -> "from <view>"; toggleHelp feeds
  lensProvenanceNote; help-lens-from css). +4.
- `909741b` bundle rebuild (41 modules, JS 38.68KB gz, CSS 10.32KB gz).

## Gates

- web `npm run check`: 747/747 tests pass (724 -> 747, +23), both tsc clean.
- Go: gofmt clean, vet clean, build clean, `go test ./...` all ok
  (commands pkg 66.7s; serve/store/model/dateparse/tui/util cached-green).
- Live `tsk serve --addr 127.0.0.1:7921 --file /tmp/t21.tsk.md`: all 8 JS hooks
  + 4 CSS hooks present in the served bundle; GET /api/tasks 200; POST
  /api/tasks/1/toggle round-tripped to .tsk.md preserving depends: + adding
  completed:; `tsk show 3` read depends:#1 back; raw .tsk.md stayed plain
  hand-editable markdown. Cleaned up the /tmp fixture after.

## Deferred

Nothing forced. Standing long-carries: F48 (context-menu submenus), F49
(autocomplete in edit/filter), F50 (dep mini-graph), F54 (touch context menu).
T22 backlog (F117-F121) appended to STATE-web.md so the loop never starves.
