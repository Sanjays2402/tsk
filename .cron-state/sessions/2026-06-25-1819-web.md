# T15 web tick — 2026-06-25 18:19 PT

Shipped 5/5 web slices (T15 queue F81-F85). Led the standing queue exactly as
written; no drops, nothing padded.

## Shipped

- **F81** `764ad66` feat(web): clickable lens-breakdown priority pills.
  renderLensBreakdown gains an `active` set -> the four priority pills become
  <button data-lens-bd-prio> drill-downs that layer the priorities facet onto the
  active lens (lens AND urgent), toggle-off on re-click. New pure helpers
  lensBreakdownPriority + LENS_BD_PRIORITIES; cross-cut pills (overdue/blocked)
  stay plain spans. +6 lens tests.
- **F82** `aa7bcd1` feat(web): lens digit echoed in the chip + help overlay.
  renderLensChipBody leads with a lens-chip-key <kbd> ("2 overdue x");
  renderActiveLensHelp() builds the ? overlay line with the digit kbd + bold
  label (replaces the plain-text path); the lens-shortcut help row bolds while a
  lens is active. +5 lens tests.
- **F83** `6bfebf1` feat(web): palette set-command disabled-reason hints.
  setCommandDisabledReason + renderDisabledReason -> a disabled Set due/priority
  command with no selection says "select a task first" in the shared preview slot
  (already-<level> keeps F77's empty-state text). paintPaletteDuePreview checks
  it first, seq-bumping in-flight due parses. +6 palette tests.
- **F84** `de50582` feat(web): all/shown scope toggle for the export menu.
  renderExportMenu gains scopeShown; scoping active -> All / N shown segmented
  toggle (data-export-scope-toggle); items carry data-export-scope mapped to
  F78's forceScope override. +4 export tests (one F75 test updated for new markup).
- **F85** `c084551` feat(web): upstream "what waits on this?" walk.
  deps.ts openDependents / deepestDependentChainFrom / hasWalkableDependents
  (reverse-edge mirrors of the blocker walk). openChainDrill takes a direction +
  a header flip button so downstream blockers and upstream dependents are both
  auditable from one popover. +8 deps tests.
- `6f6fc6e` chore(web): rebuilt embedded SPA bundle (40 modules, JS 34.32KB gz,
  CSS 9.30KB gz).

## Gates (once, end of batch) — all green

- web: npm run check 586 pass (was 557; +29 net new), npm run build ok
- gofmt -l clean, go vet clean, go build ok
- go test ./... ok (internal/commands 56.1s; serve/store/model/tui cached)

## Live proof

Built /tmp/tsk-t15, a #4->#3->#2->#1 dep chain, `tsk serve --addr 127.0.0.1:7895`.
- F84 backend: /api/export?format=csv&ids=1,4 -> exactly #1+#4 in store order;
  whole-store markdown -> all four. Both targets via one endpoint.
- Served bundle carries every T15 hook: lens-bd-prio, lens-chip-key, "select a
  task first", export-scope-toggle, data-chain-flip + "Waiting on #" + "what
  waits on this?".
- Storage contract intact: raw .tsk.md clean plain markdown with depends: meta;
  `tsk show 4` + `tsk depend 4 --tree` read the #4->#1 chain back.

## Deferred

Nothing forced. F48/F49/F50/F54 remain the standing T9 carries; T16 backlog
(F86-F90) appended to STATE-web.md so the loop never starves.

Push: f2f412c..6f6fc6e -> origin/main (clean fast-forward).
