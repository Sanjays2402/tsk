# 2026-06-22 18:39 PT — tick #24

Shipped 5/5, all single-commit, full gate green, pushed to
`origin/main` (31111a4..a12497a).

- `archive --bucket-by tag:!X`                — 31111a4
- `archive --bucket-by tag:X,Y --strict-and`  — 34a84f7
- `graph --json --output <path>`              — 961c5a0
- TUI 'N' jump-next-unblocked                 — bc92616
- TUI 'F' focus-pinned toggle                 — a12497a

38 new test cases across the 5 features, all green. ~1000+
existing cases also still green after the archive factory refactor
(parseInversionTags + makeTagFilterBucketFnInversionMode +
matchTagBucket), the strictAnd plumbing, the graph --output
dispatch reshape, and the TUI pinnedOnly filter additions.

The archive bucket-by tag axis is now complete in all four
logical combinations (positive union / positive intersection /
inverse union / inverse intersection). The graph --output cluster
is now complete (every render format + the JSON envelope all
write to disk with extension safety). The TUI gains its first
smart-selection hotkey ('N') and its first toggle-filter
primitive ('F'); the help table is now in sync with the keymap
for the first time in several ticks (N/F/r/R/C all now appear).

No deferrals. Process notes:
- The old makeTagFilterBucketFn is preserved as a thin wrapper
  over the new inversion-aware factory for name continuity even
  though no internal caller uses it directly anymore.
- TUI tuiUnmetBlockers / isBetterNextTUI are documented same-
  package duplicates of the CLI's helpers (internal/tui can't
  import internal/commands — cycle).
- Caught one near-miss: my toggleFocusPinned patch matched too
  broadly and almost dropped `func (a *App) startEditTitle() {`.
  Targeted patch restored it before the build catch — no remote
  impact, but worth noting.
