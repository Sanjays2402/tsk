# tick 2026-06-22 15:04 PT (tick #23)

Shipped 5/5 features cleanly to main. Quality gate (gofmt + vet +
build + go test ./...) green before push. Single revertible commit
per feature. 35 new test cases on top of the existing suite, all
green.

## Features shipped

1. `graph --output <path>` — 008dfd9
   feat(graph): writes the rendered graph to a file with format/
   extension validation. svg→.svg, dot→.dot/.gv, ascii→.txt or
   none. Atomic write (buffer-before-write). JSON path NOT
   supported in this slice. Case-insensitive extension match.

2. `doctor --fix-orphans --dry-run` — 904f8a5
   feat(doctor): previews dangling-ref scrubs without writing.
   Shares the prune logic with applyOrphanArchiveFix via a
   sibling previewOrphanArchiveFix. New OrphanArchiveRef type.
   Per-ref details sorted by (task_id, missing_dep) for
   reproducible CI diffs. WARNINGS retained (dry-run didn't fix).

3. TUI 'C' clone — ee6e92f
   feat(tui): clones the currently-selected task in place. Mirrors
   the CLI clone verb exactly (suffix, deep copy, fresh Created,
   done reset). Inline implementation to avoid import cycle.
   Cursor moves to the new clone.

4. `archive --bucket-by tag:X,Y,Z` — 5f8a280
   feat(archive): multi-tag CSV union partition. Tagged-any-of-
   these vs other. Label preserves user case + ordering;
   matching is case-insensitive. Permissive tokenization.
   Single-tag backward compat verified by regression test.
   makeTagFilterBucketFn now takes []string.

5. TUI 'R' reload+clear-filter — 8b8126f
   feat(tui): uppercase sister of 'r'. Clears the search filter
   before delegating to reloadFromDisk. Status footer
   distinguishes ("reloaded (filter cleared)"). No-filter case
   is no-op equivalent to lowercase 'r'.

## Files added

- internal/commands/graph_output_test.go (9 + 1 matrix)
- internal/commands/doctor_fix_orphans_dryrun_test.go (7)
- internal/tui/clone_test.go (6)
- internal/commands/archive_bucket_by_tag_csv_test.go (6)
- internal/tui/reload_clear_test.go (6)

## Files modified

- internal/commands/graph.go (flag + validation + dispatch + help)
- internal/commands/doctor.go (--dry-run flag + branch + help)
- internal/commands/doctor_fix_orphans.go (preview sibling)
- internal/commands/archive.go (signature change + CSV helper + help)
- internal/tui/keys.go (ReloadClear + Clone bindings)
- internal/tui/app.go (cloneCurrent + reloadFromDiskClearingFilter
  methods + handleNavKey wiring)

## Deferred

None. All 5 features shipped with passing tests. Roadmap items
tick #22's backlog row covered: graph --output, doctor --fix-
orphans --dry-run, archive --bucket-by tag:X,Y, TUI 'C', TUI 'R'.

Added a tick #23 backlog with 30 fresh items including:
- graph --output --watch N (re-render on file change)
- graph --reachable + --json + --output (extend file output to JSON envelope)
- archive --bucket-by tag:!X (inverse) and tag:X,Y --strict-and (intersection)
- doctor --fix-orphans --interactive + --merge-into variants
- TUI 'N' jump-next-unblocked, 'F' focus-on-pinned, 'X' archive-current
- 4 recipe docs (deep dives on common pipeline patterns)
