# T6 web tick — 2026-06-24 10:06 PT — depth (5/5)

Workdir: `/Volumes/Projects/tsk` NOT mounted (external SSD absent; `diskutil
list external` empty). Prompt's hard-coded path is stale. Worked from the
fully-synced internal worktree `/Users/sanjay/Projects/tsk-features/main`
(origin/main 378c75e, clean). Pushed clean fast-forward 378c75e..1565651.

## Shipped (5/5) — F26-F30, the T6 "depth" cluster

- F26 feat(web): dependency awareness in the UI (1eaee70)
  - DTO depends_on (omitempty) + patch setter; sanitizeDeps validator
    (dedupe, refuse self-ref, reject unknown -> 400, preserve order).
    deps.ts (12 tests): doneIndex/openBlockers/isBlocked/blockedBadge.
    Blocked rows grey title+check + "blocked by #N" badge that jumps to the
    blocker (clears filter/route first). 6 Go tests.
- F27 feat(web): pinned tasks + floating Pinned section (ae72c31)
  - DTO pinned (omitempty) + patch setter; POST /api/tasks/:id/pin toggle.
    sections.ts: new "pinned" bucket floats undone pins to the top (done
    pins stay in Done), priority-sorted. Star toggle, is-pinned gold rail,
    gold section header. Optimistic pin (p key + star + Cmd-K). 5 section
    tests + 5 Go tests. Round-trips to pin:true; `tsk top` floats it.
- F29 feat(web): inline priority cycling on the chip (f67a180)
  - priority.ts (9 tests): ladder + nextPriority/prevPriority wrap. Chip is
    a button; click raises, alt/shift-click lowers; optimistic PATCH. Cmd-K
    raise/lower. No backend change.
- F30 feat(web): search-match highlighting (1250ba4)
  - highlight.ts (13 tests): mirrors filter's fuzzy-subsequence rule, marks
    matched title chars in <mark>, HTML-safe. Threaded query via RowContext.
    No backend change.
- F28 feat(web): mobile + touch polish (dc868f0)
  - touch.ts (9 tests): long-press state machine (slop + threshold + latch).
    Long-press a row -> bulk-select (+ haptic). Bottom action SHEET on
    narrow screens; enlarged tap targets on no-hover devices. Pure frontend.
- chore 5627624: rebuilt embedded SPA bundle (31 modules, JS 19.62KB gz,
  CSS 7.05KB gz). style 1565651: gofmt struct-tag realign post-DTO additions.

## Gates (once, end of batch)

- gofmt -l clean, go vet clean, go build ok
- go test ./... ok ALL packages (serve incl. new pin/deps handlers;
  commands ~58s)
- web npm run check: 253 pass (205 + 48 new)
- web npm run build ok

## End-to-end (live binary, tsk serve)

- F27: POST /pin -> pinned:true -> .tsk.md `pin:true` -> `tsk top` floats it
- F26: PATCH depends_on:[1] -> .tsk.md `depends:1` -> `tsk show` reads back;
  self-dep 400, unknown-dep 400
- F29: PATCH priority urgent -> chip + .tsk.md updated
- SPA index 200; bundle carries data-pin, data-prio-cycle, data-dep-jump,
  section-pinned, .title mark, is-blocked, touchstart, navigator.vibrate
- Hand-editable .tsk.md storage contract intact (CLI/TUI unaffected)

## Deferred

None — all 5 slices solid. F31-F38 (T7) + F39-F46 (T8, appended this tick)
remain for future ticks.

## Note for future ticks

The `/Volumes/Projects/tsk` path in the prompt is dead (external SSD
unplugged since at least T2). The authoritative worktree is
`/Users/sanjay/Projects/tsk-features/main`. STATE.md (CLI loop) still has a
stale "resolve via the real volume" line under Conventions — harmless since
both loops now run from the internal worktree, but worth a cleanup when the
CLI loop next runs.
