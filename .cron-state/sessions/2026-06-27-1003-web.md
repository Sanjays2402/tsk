# T23 web tick — 2026-06-27 ~10:03 PT

Shipped 5/5 web slices on `main` (F122-F126), gated once, pushed clean
(c8a0856..f9cd799).

## Features

- **F122** `fa27ace` feat(web): clickable cohort breadcrumb in the help overlay
  — renderCohortHelp's history note becomes a `<button data-cohort-back>`; a
  delegated click in ensureHelpEl runs cohortBack() + toggleHelp(true). +2 tests.
- **F123** `11c6a43` feat(web): chokepoint-shift toast gains a Focus action —
  stats.chokepointShiftToast(prev,curr) -> {message, focusId}; liveRefresh hangs a
  6s "Focus" action that setCohort(focusId). +3 tests.
- **F124** `6c4dc77` feat(web): scroll a just-pinned lens chip into view —
  views.chipClippedX(chip,container); renderViewsRow scrollIntoView's a clipped
  flashed chip, guarded for the test env. +4 tests.
- **F125** `ec5420c` feat(web): unpin a lens from the chip star + Cmd-K palette —
  palette.unpinLensCommand + lensPinned reason ctx; main.unpinCurrentLens via
  removeView; star contextmenu + touch long-press. +6 tests.
- **F126** `b126df2` feat(web): active-cohort line in the stats panel —
  cohort.renderCohortPanelLine carrying data-cohort-clear; refreshStats prepends
  it; setCohort/clearCohort/cohortBack refreshStats so it stays live. +5 tests.
- `f9cd799` chore(web): rebuild embedded SPA bundle (41 modules, JS 39.61KB gz,
  CSS 10.63KB gz).

## Gates (once, end of batch)

- gofmt -l clean, go vet ./... clean, go build ./... ok, go test ./... ok
  (serve re-run clean against the fresh bundle, 2.75s).
- web `npm run check`: both tsc configs clean, 787 node --test pass (768 -> 787,
  +19), `npm run build` ok.

## Live proof

Built /tmp/t23.tsk.md (#1 with 2 waiters, #2 with 1 -> #1 biggest chokepoint),
`tsk serve --addr 127.0.0.1:7923`. Served bundle carried every hook (data-cohort-
back x3, "Biggest chokepoint moved", scrollIntoView x4, "Unpin lens"/lens-unpin,
data-cohort-clear/stat-cohort-line; CSS button.help-cohort-history + stat-cohort-
line). Toggling #1 done shifted the biggest chokepoint #1 -> #2 (the F123
scenario) and round-tripped to .tsk.md preserving every depends: link + adding
completed:; `tsk show 3` read depends:#1 back. Storage contract intact. Cleaned
up the fixture + binary after.

## Workdir note

Canonical `/Volumes/Projects/tsk` WAS mounted this tick (HEAD at c8a0856,
node_modules present) — no internal-worktree fallback needed.

## Deferred

Nothing. F48 (context-menu submenus), F49 (autocomplete in edit/filter), F50 (dep
mini-graph), F54 (touch context menu) remain the standing long-carries. T24
backlog (F127-F131) appended to STATE-web.md.
