# Web tick T17 — 2026-06-26 03:43 PT

Shipped 5/5 web slices on `main` (F91-F95), gated once, pushed clean.
Range: `2dca726..fe3004a`. Workdir `/Volumes/Projects/tsk` was mounted
(clean tree, node_modules present).

## Shipped (5 feature commits + 1 bundle)

- **F92** `7c4c13e` Biggest-chokepoint line in the dep-stats sidebar.
  deps.biggestChokepoint + Chokepoint type; stats.renderChokepoint (shares
  the F87 data-waiting-walk hook -> opens the dependent chain-drill); threaded
  through renderDepStats + renderStatsPanel (optional, back-compat). +12 tests.
- **F94** `4ad700e` "in <lens>" scope note on the TAG facet (sister of F86).
  filter.renderTagScopeNote + .ftag-scope; wired into renderFilterBar. +5 tests.
- **F95** `a1d8874` Disabled-reason hints for the bulk-edit bar menus (extends
  F89 from the palette). bulkedit.bulkPriorityDisabledReason / bulkPinDisabledReason
  + reason line; menus take optional selected[]; openBulkEdit short-circuits a
  greyed no-op click. +9 tests.
- **F91** `65f49f2` Clickable lens digit-map in the help overlay.
  lens.renderLensDigitMap emits <button>s; main.ts delegated click -> setLens +
  close; CSS button-reset. +2 tests.
- **F93** `60f55bd` Persist the active lens per-tab (sessionStorage).
  lens.LENS_KEY + parseLens (validated vs LENS_ORDER); main.ts seeds at boot +
  persistLens() on set/clear paths. +3 tests.
- `fe3004a` bundle rebuild (40 modules, JS 35.49KB gz, CSS 9.70KB gz).

## Gates (once, end of batch) — all green

- gofmt -l clean, go vet clean, go build ok
- go test ./... ok (internal/serve re-run uncached against the new bundle: 2.25s)
- web: npm run check 633 pass (was 607; +26 net new), npm run build ok

## Live proof

Built /tmp/tsk-t17, a #2/#3/#4 -> #1 chain (#1 = chokepoint, 3 waiters),
`tsk serve --addr 127.0.0.1:7897`. Served bundle carried every hook
(data-lens-legend buttons, tsk.lens, stat-chokepoint + "Biggest chokepoint",
ftag-scope, bulkedit-reason + "all already"/"none are pinned"; CSS hooks too).
/api/stats overdue:1. Toggling #1 done round-tripped to .tsk.md keeping the
depends: chain + adding completed:; `tsk show 2` read depends:#1 back. Raw file
stayed plain hand-editable markdown — storage contract honoured.

## Deferred

Nothing forced. Long-carries remain: F48 (context-menu submenus), F49
(autocomplete in edit/filter), F50 (dep mini-graph), F54 (touch context menu).
T18 backlog (F96-F100) appended to STATE-web.md.
