# T14 web tick — 2026-06-25 13:08 PT

Shipped 5/5 web slices (lens/palette/dep/export consistency cluster). Led the
standing T14 queue (F76-F80) exactly as written; no drops.

## Shipped

- **F76** `a131e06` feat(web): lens digit-key badges on the stats tiles.
  lens.lensDigit() (inverse of lensForDigit via LENS_ORDER) -> a `.lens-key`
  kbd badge on every drill tile. +8 stats / +7 lens tests.
- **F77** `c8f4746` feat(web): live "current -> new" preview for palette
  priority commands. priorityForCommandId + priorityPreviewVM +
  renderPriorityPreview; shares the F73 due-preview slot, resolved first +
  seq-bumped so a sync priority preview can't be clobbered by an in-flight
  date parse. +10 palette tests.
- **F78** `d03fbb9` feat(web): explicit scoped + whole-store export commands in
  Cmd-K. buildExportCommands(scopedCount) prepends "Export N shown as <FMT>"
  when scoping is active; exportCommandTarget decodes both id families;
  downloadExport gained a forceScope override. +11 export tests.
- **F79** `51061f9` feat(web): walk a current blocker's chain from the dep
  editor. renderDepEditor takes a `walkable` set -> corner-arrow button on
  current blocker chips that chain deeper (sister of F74's candidate affordance);
  routes through openChainDrill. +4 depedit tests.
- **F80** `bf7fdf8` feat(web): lens-aware stats breakdown. computeLensBreakdown
  (priority split + overdue/blocked cross-cuts, redundant cross-cut suppressed)
  + renderLensBreakdown ("In view" section); refreshStats appends it when a lens
  is active. +9 lens tests.
- `7088dbd` chore(web): rebuilt embedded SPA bundle (40 modules, JS 33.58KB gz,
  CSS 9.04KB gz).

## Gates (once, end of batch) — all green

- gofmt -l clean, go vet clean, go build ok
- go test ./... ok (internal/commands 57.9s; internal/serve cached vs the fresh
  T14 bundle)
- web: npm run check 557 pass (was 519; +38 net new), npm run build ok

## Live proof

Built /tmp/tsk-t14, init + 4 tasks (#1 overdue+urgent, #2 due-this-week+high,
#3 medium no-due, #4 low blocked by #1), `tsk serve --addr 127.0.0.1:7891`.
- F78 backend: /api/export?format=csv&ids=1,4 -> exactly #1+#4 in store order;
  no-ids markdown -> all four. Whole-store + scoped both via one endpoint.
- Served bundle carries every hook: lens-key, data-cmdk-due-preview + "Already",
  export-scoped-, data-dep-chip-walk, lens-bd + "In view".
- Storage contract intact: raw .tsk.md clean plain markdown with depends:1;
  `tsk show 4` reads #1 back.

## Deferred

Nothing forced. F48/F49/F50/F54 remain the standing carries; T15 backlog
(F81-F85) appended to STATE-web.md so the loop never starves.

Push: d374fdc..7088dbd -> origin/main (clean fast-forward).
