# tick #16 — 2026-06-21 15:32 PT

Shipped 5 features cleanly on the first pass — clear-shape
follow-ons from the tick #15 backlog (pause, archive --strategy
daily, topo --since --depth N, export --graph-dot --highlight)
plus the cross-project archive merge target from tick #14.

## Shipped (5/5)

- **`tsk pause`** — 02eb18b — top-level alias for `tsk stop` that
  pairs visually with `tsk start` (time-tracker idiom). One-line
  forwarder into runStartStop(false, nil); regression test asserts
  byte-identical output with stop. Aliased `hold`.
  Tests: 6 new (TestPauseClearsStarted, TestPauseIdempotent,
  TestPauseMultiID, TestPauseStopProduceSameOutput, TestPauseAliasHold,
  TestPauseRejectsDoneTask).

- **`tsk archive --strategy daily`** — a53fa95 — YYYY-MM-DD section
  grouping, finest-grained sibling of weekly/monthly. Reuses
  writeBucketedArchive + bucketFn from tick #14; new bucketByDay
  sortKey is year*10000+month*100+day, year-boundary safe.
  Tests: 6 new (TestArchiveStrategyDailyGroupsByDate, TestArchive-
  StrategyDailyOldestFirst, TestArchiveStrategyDailyUndatedBucket,
  TestArchiveStrategyDailyDoesNotRebucketExisting, TestArchive-
  StrategyDailyDifferentFromOthers, TestBucketByDayKey,
  TestArchiveStrategyBogusValueErrorMentionsDaily).

- **`tsk topo --since <id> --depth N`** — 0b453be — limit topo
  emission to N dependency layers from the anchor. Forward and
  reverse modes both supported. New limitTopoDepth helper walks
  store.Tasks adjacency to handle dangling/done-excluded deps
  correctly. --depth 0 = no limit (byte-identical to omitted).
  Tests: 9 new (TestTopoSinceDepthForwardLimitsLayer1/Layer2,
  TestTopoSinceDepthReverseLimitsPrereqLayers/TwoLayers, TestTopo-
  SinceDepthRequiresSince, TestTopoSinceDepthRejectsNegative,
  TestTopoSinceDepthBranching, TestTopoSinceDepthZeroNoLimit,
  TestLimitTopoDepthIsolated).

- **`tsk export --graph-dot --highlight <id>`** — b7caf46 — gold
  fill + bold border on a focus node so it stands out on a
  complex render. Threaded through both `tsk graph --format dot`
  and `tsk export --graph-dot` in lockstep; emitGraph signature
  grew a highlight int parameter. Highlight overrides every
  other node style. No-highlight path is byte-identical to the
  previous default.
  Tests: 8 new (TestExportGraphDotHighlightEmitsFocusStyle,
  TestExportGraphDotHighlightOnTSKGraph, TestExportGraphDotHighlight-
  RejectedOnNonGraphFormat, TestExportGraphDotHighlightMissingID,
  TestExportGraphDotHighlightWithReachable, TestExportGraphDot-
  HighlightOverridesBlockedStyle, TestExportGraphDotNoHighlightDoes-
  NotAddStyle, TestExportGraphDotHighlightSilentWhenFilteredOut).

- **`tsk archive --merge-into <file>`** — d5b956a — non-default
  archive file target for cross-project rollups. New
  resolveArchivePath helper handles ~ expansion, relative-to-
  store-dir resolution, and refuses to point at the active store
  (would corrupt the file). Bucketed strategies layer cleanly
  on top.
  Tests: 7 new (TestArchiveMergeIntoCustomPath, TestArchiveMerge-
  IntoSecondBatchAppends, TestArchiveMergeIntoRefusesActiveStore,
  TestArchiveMergeIntoWithStrategyWeekly, TestArchiveMergeInto-
  RelativePathResolvesAgainstStoreDir, TestArchiveMergeIntoDryRun-
  SurfacesPath, TestArchiveMergeIntoEmptyDefaultsToSibling).

## Quality gate

```
gofmt -w . && go vet ./... && go build ./... && go test ./...
```

All green. ~745 existing tests still pass after the emitGraph,
printGraphDOT, exportGraphDOT signature changes and the archive
strategy switch + resolveArchivePath additions.

## Push

`git push origin main` — pushed 02eb18b..d5b956a. Verified
landed via `git log --oneline origin/main | head -7`.

## No deferrals

All 5 features were a clean shape and shipped. No deferrals,
no fixup commits, no carryovers.

## Process notes

- The discipline of writing tests against the CLI surface first
  kept iterations tight; each feature was one focused commit.
- limitTopoDepth had a leftover scaffold line (conditional layer
  re-lookup based on reverse flag) — simplified before commit.
- All 5 features were drawn from "Polish & DX (added tick #15)"
  plus one carryover (merge-into from tick #14), bringing that
  subsection from 0/19 to 5/19 shipped.
- Added "Polish & DX (added tick #16)" subsection with 24 fresh
  items so future ticks have ample sized work (TUI, recurring,
  config, archive yearly/quarterly, multi-id highlight, graph
  SVG renderer, recipe docs, pause --all, etc).
