# Web tick T30 — 2026-06-28 16:17 PT

Shipped 5/5 web slices on `main`, gated once, pushed clean `5b45065..d0e1b06`.
Canonical workdir `/Volumes/Projects/tsk` mounted.

## Features (one commit each + bundle rebuild)
- F157 e2a83fc — peek-view command title carries the live count. peekCommandTitle
  folds quiet "·N" onto each "Peek view (<name>)"; wired in main peekCommands from
  the same countViewMatches resolver. +4 views tests.
- F158 580345c — recall-open-only Cmd-K command per view badgeHidesDone flags;
  recallOpenOnlyTitle + recall-open:<id> through recallViewHideDone (F152 sibling).
  +2 views tests.
- F161 fe87fee — staleSweepLabel shares "forgot N stale view(s)" across F151 toast
  + F156 command. +2 palette tests.
- F163 2772ca4 — appendStaleSegment "· N stale" on Views-row summary; same
  currentStaleCohortIds set F138/F144 use; composes after busiest. +4 views tests.
- F160 56e45ae — peek-busiest enriches a cohort busiest view with buildCohort
  waiter depth; shared busiestPeekText backs painter + runCommand. +3 views tests.
- d0e1b06 — web_dist rebuild (41 modules, JS 44.34KB gz, CSS 11.21KB gz).

## Gates
- web check: 928 -> 943 (+15), 0 fail; tsc app + test clean; build green.
- gofmt/vet/build clean; go test ./... green (commands 65s, rest cached).
- live serve: GET / 200; JS carried all new hooks; POST toggle round-tripped to
  .tsk.md ([x] + completed:, hand-edit format preserved). Fixture cleaned.

## Deferred
F159 (clickable busiest headline) + F162 (panel densest-jump) — distinct render
slices, sized on their own; carried. Standing long-carries: F48, F49, F50, F54.
T31 backlog (F159,F162,F164-F170) appended to STATE-web.md.

## Process note
F157 and F160 both live next to peekViewLabel in views.ts but commit cleanly
disjoint (peekCommandTitle vs enrichCohortPeek, separate functions). F163's
appendStaleSegment composes after describeViewsRowSummary rather than threading a
param through it — keeps F159's future busiest-span edit a separate diff region.
