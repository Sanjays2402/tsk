# T20 web tick — 2026-06-26 ~19:43 PT

Shipped **5/5** web slices on `main` (F107-F111). Gated once, pushed clean
`ca171c3..82edd7d`, verified on origin/main.

## Features (each its own revertible commit)

1. `f35e308` **F107** keyboard-focus every bottleneck from Cmd-K
   (buildChokepointFocusCommands -> "cohort-focus-<id>" runner-up commands;
   currentChokepoints; runCommand decode -> setCohort). +5 tests.
2. `4c083c6` **F108** cohort history back-stack (pushCohortHistory /
   popCohortHistory skipping dead ancestors; cohortHistory slot; Escape +
   chip `<` glyph step back; all clear paths reset it). +10 tests.
3. `4a599ae` **F109** lens provenance readout (lensProvenanceNote; "from
   <view>" note beside the lens chip). +4 tests.
4. `d735b9d` **F110** one-click pin-this-lens (pureLensViewName /
   findPureLensView; star reflecting pinned state; pinCurrentLens
   idempotent — recalls existing pin, never duplicates). +5 tests.
5. `28f910a` **F111** chokepoint trend hint (chokepointTrend /
   renderChokepointTrend; trendPrev threaded through renderStatsPanel;
   prevBiggestChokepoint slot in refreshStats). +4 tests.

Bundle: `82edd7d` rebuild (41 modules, JS 38.25KB gz, CSS 10.23KB gz).

## Gate (once, end of batch)

- Web: `npm run check` — tsc (src) + tsc (test) + node --test, **724 pass / 0
  fail** (was 700; +24). `npm run build` clean.
- Go: gofmt clean, `go vet ./...` clean, `go build ./...` clean,
  `go test ./...` all ok (commands, serve, store, model, dateparse, tui, util).

## Live smoke (`tsk serve` 127.0.0.1:7920)

Fixture /tmp/t20.tsk.md: #1 (3 waiters) + #2 (2 waiters). Served bundle
app-BLEIlhsp.js carried every T20 hook (cohort-focus-, Focus chokepoint #,
data-cohort-back, back to, cohort history cleared, lens-from, came from the
saved view, lens-pin, is-pinned, Pin the, pinned lens, chokepoint-trend,
was #, biggest chokepoint changed). POST /api/tasks/1/toggle round-tripped to
plain .tsk.md preserving every depends: link + adding completed:; `tsk show 3`
read depends:#1 back; /api/export?ids=6,7 = #2's cohort (200). Storage
contract honoured.

## Deferred

Nothing forced. Standing long-carries: F48, F49, F50, F54.

## Note

Accidentally ran the first fixture `add`s against the repo's own `.tsk.md`
(env var is not TSK_FILE; the flag is `--file`). Restored `.tsk.md` to its
single `testroot` line + removed the stray `.tsk.md.bak`; working tree clean,
only the intended commits landed.

## Next (T21 backlog appended to STATE-web.md)

F112 pinned-lens glyph in views row · F113 cohort depth badge · F114 "focus
NEW biggest chokepoint (was #M)" command · F115 pin-lens from Cmd-K · F116
lens provenance in the `?` overlay.
