# Web tick T29 — 2026-06-28 11:47 PT

Shipped 5/5 web slices on `main`, gated once, pushed clean `5c722c4..186c4f2`.
Canonical workdir `/Volumes/Projects/tsk` mounted (no internal-worktree fallback).

## Features (one commit each + bundle rebuild)
- F155 304df6d — cohort-trail "jump to densest" button (pure densestCountIndex
  argmax over the rendered F150 counts; renderCohortTrail data-cohort-densest ->
  cohortJumpTo; .cohort-trail-densest CSS). +13 cohort tests.
- F152 550e71e — clickable open/done count badge hides the done slice (pure
  badgeHidesDone; renderViewChips hideDoneBadge -> <button data-view-hide-done>;
  main recallViewHideDone = recall + setFilter hideDone; is-actionable CSS).
  Relocated badgeHidesDone next to renderViewChips so F152/F154 diff regions are
  disjoint + independently committable. +6 views tests.
- F154 dc19d78 — views-row summary names the busiest inline (describeViewsRowSummary
  optional BusiestViewLabel -> "· busiest: <name> (K)"; main wires from
  currentBusiestViewId). +4 views tests.
- F153 9226613 — peek the busiest view from the palette (pure peekBusiestViewCommand
  id peek-busiest; maybePeekBusiestViewCommand; paintPaletteDuePreview renders
  peekViewLabel; runCommand echoes, look-don't-touch). +4 palette tests.
- F156 4a9ffde — undo the last stale-cohort sweep from the palette (pure
  undoStaleSweepCommand id cohort-undo-sweep; lastStaleSweep stash past the F151
  toast; undoLastStaleSweep -> restoreSweptViews; staleSweepCount ctx +
  "nothing to restore" reason). +5 palette tests.
- 186c4f2 — web_dist bundle rebuild (41 modules, JS 44.07KB gz, CSS 11.21KB gz).

## Gates
- web check: tests 907 -> 928 (+21), 0 fail; tsc app + test clean; build green.
  Every cumulative commit-prefix re-checked independently (each feature commit
  builds green on its own).
- gofmt/vet/build clean; go test ./... green (commands 70.2s, serve cached).
- live tsk serve 127.0.0.1:7989 off the shipped binary: GET / 200; served JS
  carried all 8 new hooks (data-view-hide-done, "click to hide done",
  cohort-trail-densest, "Peek busiest view", peek-busiest, "Undo last stale
  sweep", cohort-undo-sweep, "busiest: "); POST /api/tasks/1/toggle round-tripped
  to .tsk.md adding completed:, hand-editable format preserved. Storage contract
  intact. Fixtures + binary cleaned up.

## Deferred
F157 (peek-view count in the palette command TITLE) — distinct palette-render
slice, sized on its own. Standing long-carries: F48, F49, F50, F54.
T30 backlog (F157-F163) appended to STATE-web.md so the loop never starves.

## Process note
F152/F154 share an adjacent region of views.ts (a glued close brace defeated
line-level commit splitting). Relocated badgeHidesDone above renderViewChips to
decouple the diff regions — each feature now commits + reverts independently.
Keep sibling features' helpers textually apart when they batch together.
