# T12 web tick — 2026-06-25 03:01 PT (started 03:01)

Branch: main. Origin Sanjays2402/tsk. Workdir: canonical /Volumes/Projects/tsk
WAS mounted this tick (no lock-wrapper cd failure). Was at origin/main e34fe01,
clean tree. Needed `npm --prefix web install` (node_modules partial —
@types/node absent, broke test typecheck until reinstalled). Clean FF
e34fe01..765c32c, HEAD == origin/main.

## Shipped (5/5) — sidebar lenses + keyboard depth cluster

- F66+F69 stats sidebar tiles become click-to-filter lenses — cbacb5a
- F67 "Set due" command group in the palette — 9a341f3
- F70 keyboard nav inside the chain-drill + unblock popovers — 27f06b5
- F47 bulk pin/unpin + live due preview in the bulk bar — c72f167
- F61 walk the blocker chain from the row badge — aa2fc66
- chore rebuild embedded SPA bundle — 765c32c

## F68 dropped honestly (5 real slices, not 5 + a no-op)

F68 (bulk guard parity) was a no-op on audit: the ONLY bulk completion path is
the bulkbar toggle, which already routes through the F60 guard. The F36 menu
sets priority/tag/due/pin — none complete a task. Pulled F61 from the standing
T11 backlog to round out a genuine 5.

## New/extended pure modules (node --test)

- lens.ts NEW: LensKind(blocked|overdue|today|week|nodue) + matchesLens +
  applyLens + chip metadata (13 tests). Generalizes F64's blockedOnly boolean.
- popnav.ts NEW: keyToPopNavAction + nextPopNavIndex (9 tests). Palette-style
  arrow/jk/Home-End/Enter nav for popovers.
- stats.ts: renderLensMetric + schedule tiles drill (+10); F64 blocked tile ->
  data-lens-drill="blocked"
- palette.ts: buildDueCommands (F67, +5)
- bulkedit.ts: renderBulkPinMenu + due-preview slot (F47, +2)
- deps.ts: deepestChainFrom + badge chain button (F61, +6)
- +42 net new web tests (451 -> 493).

## main.ts wiring

- F66/F69: blockedOnly boolean -> single activeLens slot; applyLens in render
  pipeline (whole-list done-index); unified data-lens-drill stats dispatch; Open
  tile -> hide-done FACET (serializes), rest -> lens; active-lens chip with hue.
- F67: buildDueCommands group in palette; runCommand dispatches due-set-clear +
  dynamic due-set-<token> via commitDue (same parse-date PATCH as F12 picker).
- F70: chain drill + unblock picker each track navIds + navIndex, paint active
  row, Enter jumps; onChainKey/onUnblockKey route through keyToPopNavAction.
- F47: openBulkEdit "pin" branch (click menu) + due live preview
  (updateBulkDuePreview, seq-guarded); bulkSetPinned via PATCH pinned setter.
- F61: openChainDrill(fromId?) — deepestChainFrom for a row, longestChainPath
  globally; row-content delegate handles data-chain-from before data-dep-jump.

## Gates (once, end of batch) — all green

- gofmt -l clean, go vet clean, go build ok
- go test ./... ok (internal/commands 62.4s; internal/serve 5.05s vs new embed)
- web: npm run check 493 pass, npm run build ok (40 modules, JS 32.09KB gz,
  CSS 8.68KB gz)

## E2E proof

Built binary, 6 tasks: #1->#2->#3 chain + due-in-2d (#4) + overdue (#5) +
no-due (#6), `tsk serve`.
- F67: PATCH #6 due:"eow" -> disk due:2026-06-28 (NL parse path)
- F47: PATCH #4 pinned:true -> pin:true, due:2026-06-27 PRESERVED (contract);
  tsk show 4 reads pinned:yes + due back
- F66/F69: stats overdue=1 (#5) / today=0 match lens tiles
- F61/F70: deepestChainFrom(#1) = [1,2,3]
- served bundle carries all hooks: data-lens-drill, lens-hue-*, "Set due:",
  due-set-clear, chain-pop-keys, data-bulk-set-pin, bulk-due-preview,
  data-chain-from, dep-chain-btn

## Deferred / next

F68 dropped (no-op, see above). T13 backlog appended (F71-F75 + carried
F65/F48/F49/F50/F54) so the loop never starves. Standing unstarted: F48-F54
(T9 backlog), F65.
