# Web tick T26 — 2026-06-28 02:20 PT

Shipped 5/5 web slices on `main` (F137-F141), gated once, pushed clean
`cf0e23a..37dc6ef` (6 commits: 5 features + web_dist rebuild).

## Features
- **F137** `03f1463` — cohort-trail keyboard nav. cohort.ts pure
  cohortTrailKeyTarget(len, dir): "step"->len-1 (one ancestor back),
  "root"->0 (drill origin), empty->-1. main.ts Alt+Left/Alt+Right wired
  before the modifier early-return, gated on focused-cohort+history+not-
  typing, routing through cohortJumpTo. HELP_ROWS row. +10 tests.
- **F140** `92b7feb` — copy-chain on the trail. cohort.ts pure
  formatCohortTrailText -> "#A › #B › #current". renderCohortTrail trailing
  data-cohort-copy button. main.ts copyCohortChain() guards
  navigator.clipboard (fallback shows the chain). app.css. +5 tests.
- **F138** `bafd9df` — stale cohort chips. views.ts pure isStaleCohortView
  (injected live-cohort check). renderViewChips staleCohort opt ->
  is-stale-cohort + tooltip. recallView self-cleans a dead cohort view
  (F134 leave-fade). app.css dims/strikes. +6 tests.
- **F139** `3386db5` — Cmd-K cohort pin/unpin. palette.ts pinCohortCommand
  (+ "Recall pinned" when pinned) + unpinCohortCommand; ctx.cohortPinned +
  reasons. main.ts split pinFocusedCohort/unpinFocusedCohort shared by the
  star + commands; wired build+run+ctx. +8 tests.
- **F141** `e9686aa` — view-chip ·N match badges. views.ts pure
  countViewMatches(view, tasks, counters) — 3 injected predicates (cohort
  id-set / filter AND lens / filter). renderViewChips matchCount opt.
  main.ts real counters over the board pool (tag route applied). app.css
  .view-chip-count. +8 tests.
- `37dc6ef` — web_dist bundle rebuild (41 modules, JS 41.73KB gz, CSS 11.01KB gz).

## Gates
- web check: 829 -> 857 tests (+28), 0 fail; tsc app + test clean
- web build green; gofmt/vet/build clean; go test ./... green (commands 67.7s)
- live `tsk serve`: GET / 200; served JS/CSS carried all new hooks; POST
  toggle round-tripped to .tsk.md preserving depends:1 + adding completed:.
  Storage contract intact. Fixtures/binary cleaned up.

## Deferred
Nothing. Standing long-carries: F48, F49, F50, F54.

## Next
T27 backlog F142-F147 appended to STATE-web.md (views-row total/busiest,
markdown copy-chain, stale bulk sweep, done-state badge breakdown, peek-view
command, densest-ancestor jump).
