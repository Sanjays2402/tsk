# Web tick T18 — 2026-06-26 08:50 PT

Shipped 5/5 web slices on `main` (F96-F100), gated once, pushed clean.
Range: `95b0a0d..6d06ce3`. Workdir `/Volumes/Projects/tsk` was mounted
(clean tree, node_modules present). SSD-absent fallback not needed.

## Shipped (5 feature commits + 1 bundle)

- **F96** `bd02cae` Cohort focus — narrow the board to a chokepoint's waiting
  cohort. New pure web/src/cohort.ts (buildCohort/applyCohort/chip). A "focus"
  button on the F92 chokepoint line (data-cohort-focus) + a filter-bar cohort
  chip. ALSO wired the previously-dead F92 data-waiting-walk in the sidebar.
  +21 tests.
- **F97** `3fa1ce6` Persist the lens-drilled priority facet across reloads.
  lens.ts LENS_FACET_KEY + parse/serializeLensFacet; main.ts persists from
  setFilter + restores at boot when a lens restored. +8 tests.
- **F98** `f46f8aa` "Clear lens" Cmd-K command. palette.clearLensCommand +
  CommandReasonContext.hasLens ("no lens active"); main.ts build/run + thread.
  +5 tests.
- **F99** `be1124c` Clickable lens scope notes. filter.ts scope notes are now
  <button data-lens-scope-clear>; main.ts clears the lens (keeps the facet).
  +3 tests.
- **F100** `bc75963` Due + tag bulk-edit reason parity. bulkedit.ts
  bulkTagCommandReason + bulkDueClearDisabledReason + exported bulkReasonLine;
  editors get a live [data-bulk-reason-slot]. +8 tests.
- `6d06ce3` bundle rebuild (41 modules, JS 36.50KB gz, CSS 9.91KB gz).

## Gates (once, end of batch) — all green

- gofmt -l clean, go vet clean, go build ok
- go test ./... ok (internal/serve re-run uncached against the new bundle: 2.26s)
- web: npm run check 666 pass (was 633; +33 net new), npm run build ok
  (deterministic — no bundle drift on rebuild)

## Live proof

Built /tmp/tsk-t18, a #2/#3/#4 -> #1 chain (#1 = chokepoint, 3 waiters),
`tsk serve --addr 127.0.0.1:7898`. Served bundle carried every hook
(data-cohort-focus, data-filter-cohort, stat-chokepoint-focus, tsk.lens.facet,
lens-clear + "Clear lens" + "no lens active", data-lens-scope-clear,
data-bulk-reason-slot + "no change to any selected" + "all already have no due").
/api/export?ids=2,3 returned exactly the cohort in store order. Toggling #1 done
round-tripped to .tsk.md keeping the depends: chain + adding completed:;
`tsk show 2` read depends:#1 back. Raw file stayed plain hand-editable markdown.

## Commit-split note

F96 and F97 interleave in main.ts (cohort state beside the lens-facet boot
restore + persistLens rewrite), so the F96 commit carries those shared
lens-restore lines; F97 owns lens.ts + tests + the setFilter hook. Each feature
is still revertible by its own module/test files; the cumulative index build was
verified byte-for-byte equal to the final tree before each commit.

## Deferred

Nothing forced. Long-carries remain: F48 (context-menu submenus), F49
(autocomplete in edit/filter), F50 (dep mini-graph), F54 (touch context menu).
T19 backlog (F101-F105) appended to STATE-web.md.
