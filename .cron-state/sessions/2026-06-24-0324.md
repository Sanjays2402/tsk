# Tick T1 — 2026-06-24 03:24 PT

**5/5 great slices shipped. All on `main`.**

| # | Feature | Commit |
|---|---------|--------|
| F1 | `chore(cron):` bootstrap `.cron-state/` + 22-item web roadmap | `7051b5b` |
| F2 | `feat(serve):` JSON API + `tsk serve` (Go net/http, reuses store) | `9923f0d` |
| F3 | `feat(web):` Vite+TS scaffold with amber/gold tokens, go:embed | `c69bc50` |
| F4 | `feat(web):` live task list (priority/due/tags, empty state) | `4f8ba43` |
| F5 | `feat(web):` click-to-toggle with optimistic update + rollback | `31e32c4` |

## Gates

- `gofmt -l .` clean
- `go vet ./...` clean
- `go build ./...` ok (binary embeds the built SPA)
- `go test ./...` ok across 7 packages, including new `internal/serve`
  (12 tests covering CRUD round-trip, disk persistence, error codes,
  stats schema, placeholder HTML, JSON validation)
- `npm --prefix web install` succeeded (typescript + vite only)
- `npm --prefix web run build` ok — 2.78KB gz JS, 2.25KB gz CSS

## End-to-end proof

Built `/tmp/tsk-test`, ran `tsk init` + two `tsk add`s in `/tmp/tsktest`,
ran `tsk serve --addr 127.0.0.1:7893`, then:

- `GET /api/health` → file path + RFC3339 now
- `GET /api/tasks` → 3 tasks serialized correctly
- `GET /` → embedded SPA HTML with hashed asset URLs
- `GET /assets/app-*.js` → 200 with `text/javascript` (proper MIME)
- `POST /api/tasks/2/toggle` → server flipped done=true + set completed
- `cat /tmp/tsktest/.tsk.md` → metadata identical to TUI/CLI format,
  byte-for-byte
- second `POST /api/tasks/2/toggle` → flipped back, completed cleared

## Deferred

Nothing this tick — 5/5 landed.

## Up next (T2)

Per the roadmap in `.cron-state/STATE.md`:

- F6 Add-task form (header bar, `n` shortcut, slim Linear-ish input)
- F7 Inline title edit (double-click or `e` on selected row)
- F8 Delete with undo (toast + 5s timer)
- F9 Sections (Overdue / Today / Upcoming / No Due / Done)
- F10 Keyboard nav (j/k, space/enter, `?` help)
