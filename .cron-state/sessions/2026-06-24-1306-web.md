# T7 web tick — 2026-06-24 13:06 PT — depth (5/5)

Workdir: `/Volumes/Projects/tsk` NOT mounted (SSD absent; lock wrapper logged
`(repo cd failed)`). Worked from the fully-synced internal worktree
`/Users/sanjay/Projects/tsk-features/main` (origin/main f6efb37, clean).
Pushed clean fast-forward f6efb37..b0e3dcb.

## Shipped (5/5) — F31-F35, the T7 "depth" cluster (all pure-frontend)

- F31 feat(web): one-line notes snippet under the row title (29c3392)
  - renderNotesSnippet in notes.ts: faded first-line preview under the title,
    carries data-notes so a click opens the editor (same delegated handler as
    the meta button). title-wrap became a column. 4 tests.
- F32 feat(web): saved-view enhancements (d9ccaa2)
  - views.ts: updateView (overwrite a view's filter), moveView (drag-reorder
    chips, before/end). router.ts: new #view/<id> route + viewHash. main.ts:
    recalledViewId drives an "update" affordance; recall writes the hash
    (suppressNextHashRoute guard); chip drag handlers; boot + hashchange recall.
    16 tests (views + router).
- F33 feat(web): live-reload polish (864f6bc)
  - live.ts: new "paused" LiveStatus + liveChangeMessage. main.ts: info toast
    (separate element, sits above the undo toast) on a live refresh; per-tab
    pause-live toggle on the clickable live pill, persisted in sessionStorage;
    stream stays connected while paused. 4 tests.
- F34 feat(web): settings reset + config export/import + reveal-meta (acf67b7)
  - new config.ts: versioned, kind-marked bundle (settings + views + theme);
    buildConfig/serializeConfig/parseConfig with validation (rejects non-JSON,
    foreign objects, junk). settings.ts: hideMeta pref (<html data-hide-meta>
    reveals the row meta on hover). main.ts: export (Blob download) / import
    (file picker) / reset (confirm) wired into the drawer config actions.
    17 tests (config + settings).
- F35 feat(web): PWA install button + honest offline banner (9757b37)
  - pwa.ts: classifyConnectivity(streamOffline, online) -> online|server|offline
    so the banner distinguishes "tsk serve restarting" from "you're offline";
    renderOfflineBanner; canInstall/isStandalone. main.ts: beforeinstallprompt
    capture -> "Install app" button injected into settings; online/offline
    window events repaint the top banner. 6 tests.
- chore b0e3dcb: rebuilt embedded SPA bundle (32 modules, JS 22.24KB gz,
  CSS 7.47KB gz).

## Toolchain change

config.ts is the first src module importing sibling pure modules, so it uses
explicit `.ts` import specifiers; tsconfig.json gained allowImportingTsExtensions
(additive, noEmit already set). Both Vite build and node --test resolve them.

## Gates (once, end of batch)

- gofmt -l clean, go vet clean, go build ok
- go test ./... ok ALL packages (serve incl. fresh T7 bundle; commands 58.9s)
- web npm run check: 284 pass (253 + 31 new)
- web npm run build ok

## End-to-end (live binary, tsk serve on :7893)

- /api/health ok; /api/tasks returned notes round-tripped
- PATCH /api/tasks/1 {notes} -> .tsk.md 6-space continuation lines ->
  `tsk show 1` reads them back (hand-editable storage contract intact)
- served bundle contains every slice's hooks (notes-snippet, data-view-update,
  data-view-drag, #view/, "changed on disk", live.paused, data-config-*,
  data-hide-meta, tsk-web-config, data-config-install, offline-banner,
  beforeinstallprompt)

## Deferred

None — all 5 slices solid. F36-F38 (rest of T7) + F39-F46 (T8) remain queued
(11 slices) so future ticks have ample sized work.
