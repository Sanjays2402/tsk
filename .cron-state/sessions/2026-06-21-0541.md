# tick #13 — 2026-06-21 05:41 PT

5/5 shipped, full gate green, pushed 911682e..44939fd to origin/main.

## Features

1. **`tsk justify [<id>] [--all]`** — 911682e
   - top-level discoverable verb (forwarder on `tsk depend <id> --justify`)
   - `--all` emits chain for every blocked task; JSON shape = `{id: [step,...]}` (object, not array)
   - 8 tests

2. **`tsk show <id> --upstream`** — 11b466b
   - sister of `--tree` for the dependents direction
   - reuses collectUpstreamDependents from `depend --upstream`
   - JSON round-trip-and-splice; `upstream` key omitted on leaf
   - `--tree` + `--upstream` mutex
   - 6 tests

3. **`tsk depend --pending`** — 55d651f
   - "now-unblocked notification queue" with --since window (24h default)
   - eligibility: open + has deps + zero unmet blockers + ≥1 recently-completed dep
   - rows annotate trigger ("unblocked by #1 at …")
   - reuses parseDurationLocal (consistent with `tsk log`/`stats`)
   - 9 tests

4. **`tsk path <a> <b> --any-direction`** — 3ce417c
   - undirected BFS for "are these related at all?"
   - reverse-adjacency map built once → keeps BFS O(V+E)
   - JSON adds `direction` field ("directed"/"any")
   - smarter not-found messages (suggest --any-direction; don't suggest it twice)
   - 6 tests

5. **`tsk top --pinned-only`** — 44939fd
   - bookmark view: restrict to pinned tasks only
   - new filterPinnedTasks helper (single-purpose, mirrors filterBlockedTasks pattern)
   - filter chain: scope → pinned → respect-deps
   - empty state = "no tasks", not a silent fallback
   - 6 tests

## Gate
gofmt -w . / go vet ./... / go build ./... / go test ./... — all green.
Full existing suite (~648 tests) + 35 new tests = all pass.

## Process notes
- TestPendingHonorsSinceWindow had a hand-edit bug: `IndexAny(rest, " -")` matched
  the `-` inside RFC3339 dates (2026-06-21). Fixed to `Index(rest, " ")`.
- TestPathDirectionFieldInJSON had a shadow of `dir` (the tmp dir) — renamed
  local to `direction` for clarity.
- Used `git commit --fixup` + `GIT_SEQUENCE_EDITOR=: git rebase -i --autosquash`
  to fold a gofmt alignment pass on depend.go into the right feature commit
  (--pending), keeping each feature as one revertible commit.

## Roadmap
tick #12 subsection: 0/15 → 5/15 done. Added a fresh tick #13 subsection
with 20 sized items so future ticks have ample work (TUI gaps, import/export,
recurring tasks, polish — show --watch, lint --dep-cycles, recur, split, wrap,
dedupe --merge, rules, timer, etc).
