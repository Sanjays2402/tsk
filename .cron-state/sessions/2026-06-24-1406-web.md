# T8 web tick — 2026-06-24 14:06 PT — depth (5/5)

Workdir: `/Volumes/Projects/tsk` NOT mounted (SSD absent; lock wrapper logged
`(repo cd failed)`). Worked from the fully-synced internal worktree
`/Users/sanjay/Projects/tsk-features/main` (origin/main 958dfcb, clean).
Pushed clean fast-forward 958dfcb..8b69c18.

## Shipped (5/5) — F36-F40, the T7/T8 "depth" cluster

- F36 feat(web): bulk edit selected — priority/tag/due (4e06989)
  - new bulkedit.ts: parseTagOps/applyTagOps ("+add -remove" grammar) + the
    priority/tag/due popover markup. main.ts: openBulkEdit popover anchored
    above the bulk bar, bulkPatch fan-out over the F16 selection. 15 tests.
- F37 feat(web): row context menu (a40af3e)
  - new contextmenu.ts: menuItemsFor (labels reflect done/pinned) + render +
    clampMenuPosition. main.ts: runRowAction = the SINGLE dispatcher shared by
    the menu, palette, and hotkeys; right-click + a reveal-on-hover "⋯" button
    (touch-reachable) open it. render.ts gains the button. 9 tests.
- F38 feat(web): quick-add upgrades (230f045)
  - quickadd.ts: depends:/dep:/needs: token (comma lists, optional #) +
    splitPasteLines/stripListMarker (multi-line paste). new autocomplete.ts:
    caret token-scan + #tag (usage-ranked) / @due suggestions + splice math.
    BACKEND: POST /api/tasks gains depends_on (dto.go + serve.go), validated via
    sanitizeDeps, atomic rollback on bad id (create_deps_test.go, 4 Go tests).
    37 web tests (quickadd + autocomplete).
- F39 feat(web): dependency editor (4d61c85)
  - new depedit.ts: buildAdjacency + canReach (iterative DFS) -> validateAddDep
    (refuses self/unknown/dup/cycle) + depCandidates. main.ts: openDepEditor
    popover (chips + add input + candidate dropdown), commitDeps PATCH. Wired
    into the F37 menu ("Edit blockers"), the palette, and a new `b` hotkey.
    18 tests.
- F40 feat(web): pinned-section drag-reorder (73ca9c3)
  - sections.ts: comparePinned now returns 0 (file order, not priority) so a
    manual pin order sticks. reorder.ts: computeSectionReorder translates a
    within-section gesture to the correct GLOBAL `before` id. main.ts: drags
    constrained to one section (cross-section rejected); pinned uses the section
    reorder. 8 reorder tests + updated sections test.
- chore 8b69c18: rebuilt embedded SPA bundle (36 modules, JS 27.19KB gz,
  CSS 8.03KB gz).

## Gates (once, end of batch)

- gofmt -l clean, go vet clean, go build ok
- go test ./... ok ALL packages (serve incl. fresh T8 bundle + create-deps;
  commands 58.5s)
- web npm run check: 358 pass (284 + 74 new)
- web npm run build ok

## End-to-end (live binary, tsk serve on :7894)

- /api/health ok
- F38: POST /api/tasks {depends_on:[1,2]} -> 201 depends_on:[1,2]; .tsk.md got
  `depends:1,2`, `tsk show 3` read `depends: #1, #2` back; bad dep 999 -> 400 AND
  rolled back (store stayed 3 tasks, no orphan)
- served bundle carries every slice's hooks (data-bulk-edit, data-bulk-set-prio,
  data-row-action, data-ctxmenu, depends:, data-ac-value, data-dep-input,
  data-dep-cand, data-section)
- hand-editable .tsk.md storage contract intact

## Deferred

None — all 5 slices solid. F41-F46 (rest of T8) + F47-F54 (T9, appended this
tick) remain queued (14 slices) so future ticks have ample sized work.
