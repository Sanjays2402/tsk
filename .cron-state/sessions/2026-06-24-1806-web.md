# T9 web tick — 2026-06-24 18:06 PT

Loop: web-frontend mission (STATE-web.md). Branch: main. Shipped 5/5.

## Shipped (5 feature slices + bundle rebuild)

- F43 highlight matched chars in tags + notes  — 2d13796
- F44 keyboard priority cycle ([ ]) + pin-to-top (shift+P) — 7c5bbbf
- F45 confirm before completing a blocked task — 4c03e24
- F46 blocked / pinned / chain-depth stats row — 0cd22d2
- F41 touch priority picker (long-press the chip) — 73ee7e8
- chore rebuild embedded SPA bundle — efd8c29

Pushed f2c64dc..efd8c29 to origin/main (clean fast-forward, verified 0/0).

## Deferred (honest 5/5, not 6)

- F42 "unblocked just now" toast — DEFERRED. It's a different shape from the
  rest (a before/after done-index diff threaded through the toggle round-trip
  + a toast-with-jump-action), not the pure-render cluster F41/F43-F46 form.
  Bundling it would have rushed it. It's now the top unstarted item for T10,
  re-listed in the T10 backlog with an implementation sketch.

## Gates (once, end of batch) — all green

- gofmt -l clean, go vet clean, go build ok
- go test ./... ok all 8 packages (internal/commands 67.3s, internal/serve with
  the freshly-embedded T9 bundle)
- web: npm run check 395 pass (was 358; +37 net new), npm run build ok
  (37 modules, JS 28.30KB gz, CSS 8.15KB gz)

## Live end-to-end proof

Built binary, set up a temp store (3 tasks, #3 depends on #1, #2 pinned),
`tsk serve`:
- F41: PATCH /api/tasks/2 {priority:urgent} round-tripped low->urgent on disk
  as `prio:urgent`, preserving pin:true + tags (hand-editable contract intact).
- F45: #3 open_blockers=[1] before, =[] after completing #1 — guard fires then
  stands down, mirroring the CLI `done` gate.
- Served bundle carries every slice's hooks (prio-pick/data-set-prio, notes-
  snippet/tag mark, Chain depth, complete-anyway/is-blocked-by, Dependencies/
  Blocked/metric-pinned).

## Workdir note

Canonical /Volumes/Projects/tsk (external SSD) NOT mounted again — only
Macintosh HD present, lock wrapper logged "(repo cd failed)". Worked from the
fully-synced internal worktree /Users/sanjay/Projects/tsk-features/main (same
origin, was at origin/main f2c64dc clean). Documented fallback since T2.

## Notes for next tick

- T10 top item is the carried F42. T10 backlog (F42,F55-F60) appended to
  STATE-web.md so the loop never starves.
- prioritypicker.ts is the second src module (after config.ts) to import a
  sibling with an explicit `.ts` specifier — required for the node --test
  runner. Any NEW src module that is directly imported by a test AND imports
  siblings must do the same; render.ts-style extensionless imports only work
  for modules that are never tested directly (bundled by Vite only).
- The highlightText engine is now generic — F57 (palette highlight) can reuse it.
