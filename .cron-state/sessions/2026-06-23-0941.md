# Tick #28 — 2026-06-23 09:41 PT

Shipped 5/5 features cleanly to main. Full gate green
(gofmt + vet + build + go test ./...). Pushed and verified
on origin/main (550b33a..7983705).

## Features shipped

1. **`tsk graph --json --include-completed`** (b64fe6e)
   Fourth opt-in node field — per-node `completed` RFC3339
   timestamp on done tasks. Composes with all other --include-*
   opt-ins + --compact-json + --append + --upstream-of.
   7 tests.

2. **`tsk graph --json --include-started`** (4dc02d7)
   Fifth opt-in node field — per-node `started` RFC3339 timestamp
   on in-progress tasks. Sister of --include-completed for the
   open side of the work-state pair. Honors done semantics
   (started clears on done; regression-tested).
   9 tests.

3. **`tsk start --all --strict-and-tag a,b`** (179ab11)
   CSV intersection-style tag filter — the THIRD bulk-action
   surface to get this primitive (after pause --all in tick #27
   and depend --pending in tick #26). Filter summary uses the
   &-form ("tag=a&b"); mutex with --tag.
   9 tests.

4. **TUI 'Y' yank-all-visible** (bd6f9d3)
   Multi-task sister of lowercase 'y' from tick #26. Copies
   every visible task as a single OSC52-clipboard block with
   blank-line separators. Respects filter/collapse/pinned-focus
   narrowing. Status reports count + size.
   10 tests.

5. **`tsk graph --json --include-all`** (7983705)
   Ergonomic shortcut — turns on all five opt-in node fields at
   once. Byte-equivalent to the long-form individual flags
   (regression-tested). Idempotent when combined with individual
   flags. Default stays minimal.
   8 tests.

## Numbers

- Commits: 5 (each independently revertible, then a small
  gofmt fixup folded via rebase --autosquash into commit #4)
- New test cases: 43
- Files added: 5 test files
- Files modified: 4 (graph.go, start.go, keys.go, app.go)
- Final gate: all 7 packages pass (commands 57.5s, tui 4.8s,
  others <1.5s each)

## Process notes

- Two graph features (include-completed + include-started) modify
  the same emitSubgraphJSON signature. Shipped as two separate
  commits using a temporary copy/strip/restore dance: cp the
  full graph.go aside, strip out include-started fields, ship
  include-completed alone, restore the full file, ship
  include-started on top. Each commit builds and tests cleanly
  in isolation — revertable independently.
- One gofmt-induced whitespace tweak in app.go landed after the
  TUI yank-all commit; folded back into commit #4 via `git
  commit --fixup` + `rebase --autosquash` so the history stays
  exactly one logical change per commit.
- The strict-and-tag flag is now COMPLETE across all three
  bulk-action surfaces (depend --pending, pause --all, start
  --all) — same flag name, same CSV semantics, same &-form
  filter summary, same mutex-with-tag rule. Surface-consistent.
- The graph JSON envelope reaches full-fat (5 opt-in fields +
  the include-all shortcut). The composability tests confirm
  every pairwise and full-stack combination works without
  interference, so the modular opt-in design holds at full
  saturation.

## Roadmap state

5 items marked done. 28+ items remain in the active polish/DX
backlog including watch-mode flags (--watch N on graph, archive,
depend, top, show), --since-date filters (start, pause, done),
the still-unstarted long-tail (recur, config file, multi-file,
import, split, dedupe --merge, timer, rules), and the unstarted
TUI structural features (detail pane, pomodoro overlay,
multi-field add form, in-session undo, elapsed-time status,
sticky wip header).
