# tick #5 — 2026-06-20 03:12 PT

5/5 shipped. Full quality gate green before push. All on feature/autoship.

## Shipped

- `yesterday` — b73dc52 — standup summary of yesterday's completions, calendar-day-anchored
- `daily`     — 0700c11 — morning briefing: OVERDUE + TODAY + UPCOMING in one screen
- `diff`      — 276f155 — diff live `.tsk.md` vs `.bak` (what `undo-last` would revert)
- `env`       — 06db477 — effective env dump for debug + scripts (paths, TZ, editor, color, runtime, TSK_* vars)
- `export --jsonl` — e257d77 — streaming JSON-lines for pipelines (also --format jsonl / ndjson)

## Quality gate

`gofmt + go vet + go build + go test ./...` all green.
Pushed e0875f9..e257d77 to origin/feature/autoship; verified via `git log origin/feature/autoship | head`.

## Friction encountered

- Disk was 99% full on `/` (only 118Mi avail). Cleaned `go-build` cache (~226Mi recovered) to make room.
  Not blocking but worth flagging to Sanjay — system disk is genuinely tight. (12Gi used / 228Gi total,
  but most of that is `/System/Volumes/Data` which is at 100%.)
- Bumped into a pre-existing TZ quirk in tsk's store: due dates persist as UTC midnight, so a same-day
  task can compare as "before local startOfDay" when the active zone has a negative UTC offset.
  Coped by ordering `IsDueToday` before `IsOverdue` in the `daily` bucket assignment AND by writing
  tests against `dueDate(±2)` instead of `±1` (`ls_due_test.go` uses the same convention).
  Did NOT fix the underlying bug — that's a store-layer change deserving its own slice.
- `rm` doesn't have a `--yes` flag — initial test assumed it did. Dropped the flag.

## Deferred

- Nothing this tick — all 5 picked items shipped cleanly.

## Roadmap status

- Per-task CLI shortcuts: 10/10 shipped (section complete).
- New views / scriptable surfaces: 9/9 shipped (section complete).
- Storage/model/format: 1/6 shipped (jsonl). Next tick can start `tsk import`, recurrence, started-state.
- Added new "Polish & DX" section (10 items) so the next 3+ ticks have fresh sized work.
