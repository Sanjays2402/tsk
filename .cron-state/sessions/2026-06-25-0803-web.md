# T13 web tick — 2026-06-25 08:03 PT

Repo MOUNTED at /Volumes/Projects/tsk (lock wrapper clean, no cd failure).
HEAD == origin/main c474c99, clean tree, node_modules present. Clean FF push.

## Shipped 5/5 (F72 dropped as a no-op — audited, see below)

- F71 16e612c feat(web): number-key lens shortcuts (1-5) + active-lens readout in `?`
- F73 3cf0d6a feat(web): live due-date preview in the palette "Set due" group
- F75 fa42c82 feat(web): export only the visible lens/filter subset (?ids= backend)
- F74 1db180c feat(web): walk a prospective blocker's chain from the dep editor
- F65 978b66a feat(web): highlight the search match while walking blocker chains
- chore 4050acb: rebuilt embedded SPA bundle (JS 32.72KB gz, CSS 8.85KB gz, 40 mod)

## F72 dropped (honest 5, not 5+no-op)

F72 = "make the active lens survive a view recall". Audit: recallView ALREADY
leaves activeLens untouched (only clear-all / jump-to-hidden / the chip clear
it). The lens already survives a recall + stays visible via its filter chip.
The described bug doesn't exist — shipping F72 = inventing a fix. Pulled carried
F65 (pairs with F74's chain work) for a real 5. Same call as T12's F68 -> F61.

## Backend change this tick (F75 only)

internal/serve/export.go: optional ?ids=1,2,3 narrows export to that subset in
STORE order (filterTasksByIDs; skip unknown/malformed; blank -> empty). json/csv/
markdown all honor it. + export_scope_test.go (6 endpoint + 1 unit).

## Gates (once, end of batch) — all green

- gofmt -l clean, go vet clean, go build ok
- go test ./... ok ALL packages (internal/commands 59.5s; serve incl. new ids path)
- web: npm run check 519 pass (was 493; +26 net new), npm run build ok

## E2E proof

Built binary; 4 tasks w/ #1->#2->#3 chain, overdue #1, due-this-week #4; tsk serve.
F75: no-ids -> all 4; ids=1,4 -> #1+#4 store order; ids=3 md -> just "fix the bug";
empty ids -> null; ids=999,2 -> only #2. F73: parse-date eow -> Sun Jun 28 (in 3d),
1w -> Thu Jul 2 (in 7d). Bundle carries every slice hook. .tsk.md byte-clean,
depends: chain intact, `tsk show 1` reads #2 back — storage contract preserved.

## Roadmap

66 of 75 F-items shipped (F68 + F72 dropped as no-ops). Unstarted: F48 (ctx
submenus), F49 (autocomplete in edit/filter), F50 (dep mini-graph), F54 (touch
ctx menu). T14 backlog appended (F76-F80 + the 4 carried) so the loop never starves.
