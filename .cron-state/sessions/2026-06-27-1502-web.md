# Web tick T24 — 2026-06-27 15:02 PT

Shipped 5/5 web slices on `main` (F127-F131), gated once, pushed clean
`2ef41be..0b18761`.

## Features
- **F127/F128** `d2d602b` — panel cohort line (F126) gains a leading back-step
  button (data-cohort-back, only when historyDepth > 0; ‹N depth badge) + a
  trailing "walk" button (data-waiting-walk -> openChainDrill dependent). cohort.ts
  renderCohortPanelLine(focus, historyDepth=0) emits 3 disjoint sibling buttons in
  a .stat-cohort-row; main.ts passes cohortHistory.length + routes data-cohort-back
  -> cohortBack first. +6 cohort tests.
- **F129** `0e2bf6f` — unpin fades the chip out (inverse of F119 pin-flash).
  views.ts UNPIN_EXIT_MS + canAnimateChipExit; main.ts unpinCurrentLens marks
  is-unpinning then defers removeView; app.css view-chip-unpin keyframe +
  reduced-motion fallback. +3 view tests.
- **F130** `a083743` — shift-toast Focus action also opens the stats panel.
  stats.ts shouldRevealStatsOnFocus(statsOpen); main.ts toast run() chains
  setCohort + guarded toggleStats(true). +1 stats test.
- **F131** `8573e06` — `*` key toggles pin/unpin on the active lens. lens.ts
  lensPinToggleAction(kind, pinned); main.ts `*` keydown case + HELP_ROWS entry.
  +3 lens tests.
- `0b18761` — bundle rebuild (41 modules, JS 39.93KB gz, CSS 10.75KB gz).

## Gates
- web tests: 787 -> 799 (+12), 0 fail
- tsc app + test/tsconfig.json: clean
- gofmt clean / go vet clean / go build clean / go test ./... all ok
  (commands 68.1s, serve re-run 2.68s vs fresh bundle, rest cached-green)
- live `tsk serve --addr 127.0.0.1:7924`: GET / 200, /api/tasks ok, served
  bundle carried all hooks; POST /api/tasks/1/toggle round-tripped to .tsk.md
  preserving depends: links + adding completed:; `tsk show 3` read depends:#1
  back. Storage contract intact. Fixture + binary cleaned up.

## Deferred
Nothing. Standing long-carries: F48 (context-menu submenus), F49 (autocomplete
in edit/filter), F50 (dep mini-graph), F54 (touch context menu).

## Next
T25 backlog F132-F136 appended to STATE-web.md (cohort trail in panel, pin
cohort as view, chip-× delete fade, toast Focus keyboard mirror, star keyboard
pin).
